{
  "name": "cairn-monorepo",
  "private": true,
  "license": "MIT",
  "workspaces": ["core", "cli"],
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "npm test --workspaces",
    "typecheck": "npm run typecheck --workspaces && npm --prefix app run typecheck",
    "build": "npm run build --workspaces && npm --prefix app run build:vite"
  }
}
