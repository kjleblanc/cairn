# Task 229 report - show an inert Builder proposal review card

**Lane:** A (the main checkout). **Base commit:** `ed8cd4f`.
**Brief claim commit:** `6913b5b`.

## Outcome

Task 229 adds a lab-only, owner-facing review card for Task 224's inert Builder
responses. One fixed synthetic scenario shows an exact bounded tracked-text
replacement; the other shows an `external-reference` capability request. Both
cards state that they are proposals only: Cairn has not applied, executed,
published, verified, or checked the suggestion. The component has no action
control or callback and does not represent a terminal result.

A Main-owned pure composer accepts only the exact live Task 224 context object
and the response parsed against that same object. It projects that custody into
deeply frozen exact-key display data, joining replacement before-text from the
selected context. The browser-safe lab constants are independently rebuilt in
the unit suite from genuine fixed synthetic Task 224 context/response brands
and must match those projections byte-for-byte. The lab then displays only a
deeply frozen structural clone, which carries no Task 224 brand or effect
authority.

The surface remains product-dark. No production conversation, result card,
store, pending-run record, Main route, preload/IPC API, phone route, provider
transport, activation, candidate publisher, verifier, command runner, or
filesystem/Git writer consumes the projection. A later task would need to
design an authenticated nonterminal conversation/store/IPC route before this
could become a production feature.

The final proof repairs changed only the unit and browser tests. A positive
TypeScript AST rule now permits only the component's two known inert `.map()`
render loops and rejects every other call, constructor, assignment, update,
async expression, dynamic module form, CommonJS surface, `import.meta`, hidden
runtime builtin lookup, `ref`, label association, event/DOM API, `eval` and
`Function`. Causal mutants cover those families. The Edge test observes page
creation and requests from before navigation through the final interactions,
permits only the exact local lab asset families, and proves the interactions
add no request, popup, focus target or navigation.

The final focused check passes 7/7 and the installed-Edge check passes 1/1.
Independent architecture, security and visual reviews all returned CLEAR on
the final stable bytes. The complete App suite result is recorded below.

## Files touched

The 17 final implementation paths are:

- `core/src/builder-intercom.ts` adds
  `builderTurnResponseMatchesContext`, a pure identity check proving that one
  branded response belongs to the exact live branded context that parsed it.
- `core/src/index.ts` exports that one pure check.
- `core/test/builder-intercom.test.ts` proves exact object custody, clone/raw/
  Proxy/accessor refusal, cross-context refusal, and the Core export surface.
- `app/src/shared/builder-proposal-review.ts` defines the closed output-only
  projection, fixed proposal-only copy, and five fixed capability labels.
- `app/src/main/builderproposalreview.ts` composes a projection from genuine
  Task 224 custody and has no effect authority.
- `app/src/renderer/components/BuilderProposalReview.tsx` renders literal text
  from one `review` prop with no callback or action control.
- `app/lab/builderproposal-fixtures.ts` holds two deeply frozen browser-safe
  expected projections; the unit suite binds them to genuinely composed fixed
  synthetic Task 224 projections.
- `app/lab/builderproposal.tsx` structurally clones, re-freezes and renders the
  two lab fixtures.
- `app/lab/builderproposal.css` owns all card/page styling so production
  `app.css` remains dark.
- `app/lab/builderproposal.html` adds the standalone lab entry and synthetic
  badge.
- `app/lab/controls.ts` links the existing lab controls to the new page.
- `app/vite.lab.config.ts` adds the dedicated lab build input.
- `app/tsconfig.unit.json` includes the composer, shared projection, component
  and JSX support in the existing App unit build.
- `app/tests-unit/builderproposalreview.test.ts` exercises custody, exact
  projection, all five categories, literal rendering, clone inertness, source
  darkness and causal source mutants.
- `app/playwright.builderproposal.config.ts` provides the installed-Edge,
  single-worker, fixed-port lab browser harness.
- `app/tests-qualification/builder-proposal-review.browser.spec.ts` checks the
  real DOM, no action/focus/navigation, wide and compact layout, and captures
  the rendered page. Its final form also proves zero popup/new page, an exact
  whole-lifetime local-request allowlist, and zero request caused by clicks,
  double-click or Enter.
- `app/tests-qualification/builder-proposal-bundle-dark.test.mjs` proves fresh
  production JS/CSS and package/config entries stay dark, with the lab bundle
  as a positive control.

`docs/ai-work/tasks/229-brief.md` was created and committed alone at `6913b5b`
to claim the task. This report records the completed or stopped result, and
`docs/ai-work/LOG.md` receives one matching row.

One transient implementation mistake put Task 229's card CSS into
`app/src/renderer/app.css`, which would have made the production renderer
bundle carry the styling. Those bytes were removed and the file was restored
to its starting contents; the final style lives only in
`app/lab/builderproposal.css`. An early standalone visual-test draft at
`app/tests-qualification/builder-proposal-review.visual.spec.ts` was removed
after the dedicated Edge configuration/spec replaced it. Neither transient
path belongs in the final implementation manifest.

Verification regenerated ignored `core/dist/**`, `app/dist-unit/**`,
`app/.vite/build/**`, `app/.vite/renderer/**`, and `app/.vite/lab/**`.
Playwright wrote ignored `app/test-results/.last-run.json` and
`app/test-results/task229-builder-proposal-review.png`. The retained screenshot
is 708,318 bytes with SHA-256
`55E1E96069DAD1CC24D44661A7D8B68F2CC3CEEAA9799CF198217291E5AFBA6A`.
The production build's existing asset-copy step may also refresh ignored
mirrors without changing their tracked bytes. Four ignored lab-server capture
logs also remain under `app/.vite/`: `task229-lab.stdout.log`,
`task229-lab.stderr.log`, `task229-final-lab.stdout.log`, and
`task229-final-lab.stderr.log`. The summary-format App run additionally wrote
`app/.vite/task229-final-full-app.tap`. No app token or listener remains.

A read-only post-run inventory found 1,368 retained `cairn-*` directories in
`C:\Users\KenJL\AppData\Local\Temp\` with creation times between 17:56:26 and
19:17:08, after the Task 229 claim. The minute-level distribution and names
align with seven complete or attempted App compatibility runs: seven groups of
195 ordinary fixtures plus three `cairn-q9-*` entries. This is strong temporal
correlation, not proof of individual ownership or causal attribution. The
directories are outside the repository and were not deleted. Any cleanup needs
an exact ownership inventory, an owner-approved target/effect/recovery packet,
and a separate task.

Ordinary task bookkeeping used Git to claim the brief and will inspect, stage
the exact task paths, and create the local completion commit if every final
check passes. That repository activity is separate from the proposal card's
runtime behavior; it is not evidence that a proposal can write Git or authorize
an effect. No broad claim is made that verification performed no file, process,
browser, token-directory, port, or permission activity: those effects and their
cleanup are recorded below.

## Check results

### `c1` - the review projection preserves exact Task 224 custody: PASSED

Core's new identity check reads only the module-private WeakSet/WeakMap custody
created by the existing Task 224 composers. Equal canonical bytes and hashes
are insufficient: a separately composed context does not own the response.
The Main composer refuses raw or cloned values, wrong run/turn, byte-identical
other contexts, cross-context replay, accessors, Proxies and extra-key objects.

Replacement projections exact-join path, before SHA-256 and selected before
text, preserve the canonical response order, recompute neither authority nor a
plan, and expose only deeply frozen display fields. A structural clone renders
identically but cannot re-enter the custody seam.

The final 7/7 focused suite and the final architecture/security CLEAR reviews
independently confirm those custody joins and clone/cross-context refusals.

### `c2` - proposal rendering is literal and inert: PASSED

The component accepts exactly one `BuilderProposalReviewV1` prop. Every
Builder-controlled value appears in an ordinary React text child. Hostile HTML,
Markdown-like links, tags and handler text render escaped; SSR/source checks
find no script, image, SVG, style, link, form, input, button or handler seam.
The retained browser DOM contained no actionable/focusable descendant, clicks,
double-click and Enter on the suggested-target text did not navigate, and the
active element remained outside both cards.

The exact module/runtime namespace checks, positive component-body AST purity
rule and causal mutants reject hidden `ref`, label/form association, fetch,
storage, eval, dynamic/default/side-effect/CommonJS imports and exports, and
runtime builtin lookup. The final Edge run observed no popup/new page, no
unexpected request and no interaction-time request.

### `c3` - both proposal kinds are complete and honest: PASSED

The replacement card shows the fixed warning, Builder-attributed summary,
exact synthetic path, before/after SHA-256 and before/after text. Capability
projection tests cover all five closed Task 224 categories and fixed Cairn
labels; the visible scenario shows `what`, `why`, expected effect, data
exposure, cost basis, recovery and an explicitly `Untrusted suggestion` target.

Both cards say `Proposal only — Cairn has not applied, executed, published, or
verified this suggestion`, followed by `Nothing changed. No command ran` and
the explicit currentness/correctness/safety caveat. They claim no approval,
execution, verification, application or terminal task success.

The focused 7/7 suite covers both complete projections and all five capability
categories; the complete App result below found no regression.

### `c4` - the owner can inspect the card while production stays dark: PASSED

The standalone `/lab/builderproposal.html` page shows both unmistakably
synthetic scenarios in Cairn's paper/garden visual grammar. The main lab's
`Builder proposals` link opens it. At 1,280 px the scenarios sit in two columns;
below 1,120 px they stack, and at 600 px the before/proposed comparison becomes
one column without horizontal overflow. The retained compact screenshot was
visually inspected and both complete cards remained legible.

The source consumer scan permits only the shared projection definition, dark
Main composer and output-only component under `app/src`. Main/preload/IPC,
Chat, conductor store, renderer entry, production CSS, package/config and Core
review-projection surfaces stay dark. A fresh production build contained no
Task 229 marker; the lab bundle positive control did.

The lab, production and darkness builds/tests passed; the visual and
architecture reviews returned CLEAR on the final stable layout and bundle
boundary.

### `c5` - the card cannot cause or authorize an effect: PASSED

The projection contains no protocol brand, callback, action id, URL, argv,
command, approval, grant, reservation or candidate authority. The composer and
component import allowlists remain narrow. The component has no filesystem,
Git, process, native, network, provider, credential, environment, navigation or
DOM-action seam. Normal Codex/Kimi capabilities remain `serial-task`, the only
candidate capability remains the existing guarded Q9 fake adapter, both
production preview identities remain `null`, and the activation literal array
remains empty.

The browser page used only a local fixed lab server and synthetic data. Its
suggested target remained literal text. The first browser attempt refused
before loading a page because bundled Chromium was absent; no browser was
installed. The final harness selected the already installed Microsoft Edge.

The final positive AST guard and causal mutants close render-time effect and
module-authority families. Edge observed the whole page lifetime and all three
interactions without an unexpected request or new page. Security returned
CLEAR on unit hash
`D2C9E742ED1E7133CE965DAB5D41FB8A7CE36F9874B517CB334220FC396C3DB7`
and browser-test hash
`A20AB6A05C90760E4F2DA4D33C0DA297C29462626F372F56CDA5C0385AE1754D`.

### `c6` - verification, owner-visible evidence and records are complete: PASSED

The focused Core check, App typecheck, final focused App review suite, lab
build, production build, bundle darkness qualification, and installed-Edge
browser run passed as recorded below. The retained screenshot was visually
inspected directly, and an in-app Browser inspection independently confirmed
the two cards, DOM inertness and stable final style bytes.

The first browser attempt failed honestly before page load because no bundled
Playwright Chromium existed. No dependency or browser install was attempted.
After the Edge run, its test itself passed, but the surrounding PowerShell
cleanup wrapper emitted a `Test-Path` argument-parsing error. A separate exact
readback proved both app-token directories absent, port 7398 not listening and
the screenshot present. That wrapper error is not reported as a Playwright
failure or silently omitted.

The final repaired focused suite, complete App suite, exact diff/status/staging,
three independent CLEAR reviews, screenshot inspection, this report, the LOG
row and the local completion commit provide the evidence.

## Exact commands and observed results

From `core/`:

`npm.cmd run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test dist\test\builder-intercom.test.js`

Final result: exit 0; Core build passed; 10 tests passed, 0 failed; duration
96.6916 ms.

From `app/`:

`.\node_modules\.bin\tsc.cmd -p tsconfig.unit.json --pretty false; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test dist-unit/tests-unit/builderproposalreview.test.js`

Final repaired result: exit 0; unit TypeScript passed; 7 tests passed, 0
failed, skipped or todo; duration 340.3499 ms. The final unit-test source
SHA-256 is
`D2C9E742ED1E7133CE965DAB5D41FB8A7CE36F9874B517CB334220FC396C3DB7`.

`npm.cmd run typecheck`

Final result: exit 0.

`npm.cmd run build:lab`

Result: exit 0; Vite transformed 107 modules and emitted the dedicated ignored
lab bundle.

`npm.cmd run build:vite`

Result: the sandboxed attempt failed because esbuild could not read the Vite
config. The exact approved local retry exited 0: Main built 104 modules,
preload 1 module and renderer 77 modules.

`node --test tests-qualification/builder-proposal-bundle-dark.test.mjs`

Final result after the final test-only repair: exit 0; 1 test passed, 0 failed;
duration 60.2263 ms. Fresh production
JS/CSS and package/config entry surfaces contained no Task 229 marker; the lab
bundle positive control did.

`npx.cmd playwright test --config playwright.builderproposal.config.ts`

Initial result while the config used default Chromium: exit 1 before page
load because Playwright's bundled Chromium executable was not installed. No
browser install was attempted. Both app-token directories were released and
port 7398 was closed.

An earlier installed-Edge run used the same exact command with
`%TEMP%\cairn-app-token` and `app\.app-token` acquired by the wrapper.
Playwright itself exited 0; 1/1 test passed in 4.0 seconds (6.1 seconds total).
It wrote
`app/test-results/task229-builder-proposal-review.png`, 708,318 bytes, SHA-256
`55E1E96069DAD1CC24D44661A7D8B68F2CC3CEEAA9799CF198217291E5AFBA6A`.
The wrapper then emitted the disclosed `Test-Path` argument-parsing error; the
separate readback proved both token directories absent, port 7398 not
listening, and the screenshot present. That wrapper defect was repaired before
the final run.

Final repaired installed-Edge result: the same command first refused in the
sandbox because esbuild could not read `vite.lab.config.ts`. The exact approved
local retry exited 0; 1/1 test passed in 877 ms (3.9 seconds total). It proved
zero new page, zero non-allowlisted whole-lifetime request, zero
interaction-time request, no navigation and no card focus target. Exact
readback showed both app-token directories absent and port 7398 closed. The
screenshot remained 708,318 bytes with the same SHA-256. The final browser-test
source SHA-256 is
`A20AB6A05C90760E4F2DA4D33C0DA297C29462626F372F56CDA5C0385AE1754D`.

`.\node_modules\.bin\tsc.cmd -p tsconfig.unit.json --pretty false; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test --test-reporter=dot dist-unit\tests-unit\*.test.js`

Final result: exit 0. The dot reporter completed without a failure. An exact
summary-format rerun used:

`node --test --test-reporter=tap --test-reporter-destination=.vite\task229-final-full-app.tap dist-unit\tests-unit\*.test.js`

Result: exit 0; 860 tests, 858 passed, 0 failed or cancelled, 2 skipped, 0
todo; duration 279286.4913 ms.

The retained OS-temp compatibility residue was inventoried read-only with:

`$claim=[datetimeoffset]::Parse((git show -s --format=%cI 6913b5b)).LocalDateTime; $rows=@(Get-ChildItem -LiteralPath ([System.IO.Path]::GetTempPath()) -Directory -Force -ErrorAction SilentlyContinue | Where-Object { $_.Name -like 'cairn-*' -and $_.CreationTime -ge $claim }); Write-Output ('count=' + $rows.Count); $rows | Group-Object { $_.CreationTime.ToString('yyyy-MM-dd HH:mm') } | Sort-Object Name | Select-Object Name,Count; $rows | Measure-Object CreationTime -Minimum -Maximum`

Result: 1,368 current directories, earliest 17:56:26 and latest 19:17:08.
None was deleted; individual ownership was not proven.

From the repository root after exact-path staging:

`git diff --cached --check; git diff --cached --name-status; git diff --name-status; git ls-files --others --exclude-standard; git status --short --branch`

Result: cached whitespace check exited 0. The index contained exactly the 17
implementation paths listed above, this report, and one LOG modification: 19
paths total. The unstaged and untracked path lists were empty. Status showed
only those exact staged paths; the Task 229 brief remained isolated in claim
commit `6913b5b`.

## How to try it

From `app/`, run:

`npm.cmd run lab`

Open the URL Vite prints, then choose `Builder proposals` in the lab controls,
or open `/lab/builderproposal.html` directly. The page is entirely synthetic.
There is nothing to approve or apply: compare the replacement card's exact
before/proposed text and hashes with the capability card's complete disclosure.
Stop the lab server when finished. This command starts a local process and
listening port and may regenerate ignored lab output; it does not make the card
a production route.

## Limitations and next safe task

This task supplies review presentation, not a working production Builder. It
does not persist a proposal, select real project text, call a model/provider,
open a suggested target, apply a replacement, grant a capability, publish a
candidate, run verification, or emit a terminal result. The identity hashes
and synthetic task numbers on the lab page are fixed display examples.

A later production task must separately design and review an authenticated
nonterminal conversation/store/IPC route. It must not reuse `ResultCard`, add
an action control without a new exact owner decision, or treat this inert
projection as plan/application authority. Task 223's isolation STOP, Task
225's live-writer STOP and Task 228's durability STOP remain unchanged.

No dependency/browser install, provider/model/credential use, paid call,
remote service write, publication, push or deployment was performed for Task
229. Local verification did start build, browser and lab-server processes,
create/remove token directories, bind/release port 7398, and write ignored
build/test artifacts as disclosed. It also left the temporally correlated
candidate OS-temp fixtures described above; Cairn preserved them rather than
guessing at ownership or deleting outside-repository data. Ordinary brief and
completion Git commits are local task-record activity, not proposal runtime
effects. The milestone did not move.

**Disposition: DONE**
