# Task 188 — verified result receipt

**Lane:** E

**Base commit:** `b28e78784ab1a3327282c6b9672c771c3e36484d`

## Visible outcome

The envelope's finished result reads as one calm, trustworthy paper receipt
inside the conversation instead of another dense rounded dashboard card.
Evidence pictures and the DONE, STOPPED, or error disposition lead. Cairn's
checked facts remain plainly distinct from the builder's account, and the
original request remains available as context without competing with the
answer.

The receipt uses the same faded dusk paper, restrained grain, clean registration
rules, and controlled corners as the approved conversation. Its hierarchy
should let a beginner answer three questions quickly: what happened, what did
Cairn actually verify, and what can I safely do next? Paths and secondary run
detail become a quiet footer rather than a field of pills or nested cards.

## Boundary of intent

- This is the result-card presentation and information-hierarchy slice only.
  Result construction, evidence custody, accepted-request storage, conductor
  commentary, retry, reveal-in-folder, push, recovery, conversation, task,
  provider, worker, and dispatch behavior stay exact.
- Evidence pictures remain first. Disposition, checked result facts, changed
  files, saved-snapshot truth, recovery facts, and actionable controls remain
  visible and readable. Builder claims and original request may use clearly
  named native disclosures, but their complete text remains accessible and
  their provenance may not blur into Cairn-checked facts.
- DONE, STOPPED, and error receipts stay visually distinct without relying on
  color alone. Keyboard order, native disclosure semantics, accessible names,
  focus, scroll containment, compact layout, and reduced motion remain intact.
- Do not redesign proposals, dispatch confirmation, questions, connection
  setup, composer, rail, pond, or cast faces. Add no dependency, asset,
  animation, scribbled edge, or faux handwriting.
- No Core/CLI/phone source, credential, real provider call, worker call, push,
  publish, deployment, production system, or production data.
- Main's stopped Task 180/183 work and dormant lanes are protected. This task
  writes and commits only inside Lane E and does not land while main is dirty.

## Checks

1. Add a red-first renderer contract for receipt hierarchy, explicit checked-
   versus-reported language, paper treatment, native disclosures, visible
   recovery/actions, compact containment, and no new motion.
2. Run the focused result/evidence renderer tests, the full App unit suite,
   typecheck, and both production builds.
3. With the owner's app closed and both app-token locks held, use Cairn's
   scripted local worker/conductor fixtures only to exercise and capture a
   verified DONE receipt. Also mechanically verify STOPPED/error distinctions,
   disclosure keyboard behavior, action visibility, evidence ordering, compact
   containment, and reduced motion. No real provider or worker may be called.
4. Inspect the settled DONE receipt and its expanded provenance state, then ask
   the owner for the remaining taste judgment.
5. Run `git diff --check`, inspect the exact diff and final Git status, and
   confirm the main checkout's protected work has not changed.

## DONE / STOPPED

**DONE** means a finished run visibly reads as a calm verified receipt, a
beginner can distinguish Cairn's checks from builder claims and request context,
all evidence/recovery/actions remain honest and accessible, every mechanical
check passes, the owner confirms the visual direction, and only isolated Task
188 paths are committed in Lane E.

**STOPPED** means the receipt hides or blurs provenance, suppresses evidence or
recovery, becomes another nested dashboard, changes workflow/accessibility
behavior, a required check cannot be repaired in scope, protected work changes,
or the owner does not confirm the visual result.
