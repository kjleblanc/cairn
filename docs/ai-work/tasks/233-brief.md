# Task 233 brief - one approved live proposal-only Builder call

**Lane:** A (the main checkout). **Base commit:** `fab1403`.

## Requested visible outcome

In one freshly identity-owned disposable Git repository and disposable Cairn
profile, the owner personally enters an existing OpenRouter credential through
Cairn's official UI. Cairn Main selects the fixed current tracked text in
`examples/synthetic/greeting.ts`, composes the exact Task 224 context, sends it
through one injected tool-free live transport to OpenRouter model
`moonshotai/kimi-k2` with NovitaAI as the only permitted upstream, parses the
one bounded response against that exact live context, and passes the genuine
pair to Task 231's existing `appendBuilderReviewTurn` custody boundary.

Real desktop Chat shows exactly one inert **Builder proposal - not applied**
card and restores the same authenticated display turn exactly once after a cold
relaunch. The selected file and Git state remain unchanged. The route stays
evidence-only and unavailable during an ordinary launch.

The owner approved this exact paid and data-bearing action on 2026-08-14: one
OpenRouter `moonshotai/kimi-k2` request, the stated fixed synthetic data shared
with OpenRouter and pinned NovitaAI, use of the credential the owner personally
enters, and spending up to USD 0.05. Stop if any boundary cannot be proven.

## Boundary of intent

- Add only a positively guarded, injected, proposal-only live transport and
  the minimum evidence harness needed to join Task 232's selector to Task 231's
  append boundary. Keep the deterministic Task 232 fake for local causal tests.
  Do not add a normal product entrance, Builder IPC, renderer control, worker,
  verifier, native helper, command/tool/function-call surface, file applier,
  candidate/ref/index/object publication, approval/grant, Task 227 activation,
  push, deployment, dependency, retry, resume, continuation, redirect, or
  fallback.
- The only paid or project-data-bearing request is one HTTPS POST to
  `https://openrouter.ai/api/v1/chat/completions` for exact model
  `moonshotai/kimi-k2`. Its provider controls admit only NovitaAI, disable
  fallbacks, deny data collection, require a zero-data-retention endpoint, and
  require every requested parameter. The request carries no `tools`, plugins,
  web search, URLs to fetch, callbacks, response actions, or ambient history.
  Keyless public-catalog preflight GETs may carry no project, personal,
  profile, credential, or request data.
- The request may contain only one fixed proposal-only instruction and the
  exact Task 224 context derived from the disposable repository: the fixed
  synthetic selected path/current content, fixed Task Spec and Evidence Plan,
  criterion order, consent revision, task/run/turn coordinates, disposable
  project/Git hashes and selector provenance. It carries no Cairn source,
  owner's real-project text, owner conversation, task records, full local path,
  credential, environment, provider history, phone/LAN data, or other file.
- Main accepts only an exact saved OpenRouter connection whose model is
  `moonshotai/kimi-k2` and whose credential the owner entered in the disposable
  profile through Cairn's UI. The AI never opens, drives, reads, prints, copies,
  exports, logs, or serializes that credential or login. OAuth/key creation,
  account changes, purchases, top-ups, and any other authentication route are
  outside this approval.
- Spend at most one client request, before the async/network boundary. Set
  `max_tokens` to 1,024 and no retry. Immediately before sending, verify the
  keyless official catalog still exposes the exact model/upstream, zero-data-
  retention route, and a conservative current-price upper bound below USD
  0.05 for the bounded request. If catalog, route, request size, connection,
  identity, selected bytes, Git state, or price cannot be proven, send nothing.
  Once a paid request starts, any cost is already spent and no second attempt
  is authorized, even after timeout, refusal, malformed output, or transport
  failure.
- Parse only one complete bounded textual Task 224 response. It must match the
  exact live context and propose only the selected path. Malformed, truncated,
  extra, tool-call, refusal, mismatched, oversized, or ambiguous output is a
  STOP with no append and no retry. Raw prompt framing, context object,
  provider envelope, credential, headers, brands, and transport authority
  remain in Main memory only and do not enter transcript, marker, IPC,
  renderer, screenshot, provider history, phone/LAN state, logs, report, or
  restart evidence. Only Task 231's inert authenticated display projection -
  including its bounded summary and selected-before/proposed-after text - and
  non-secret receipt facts may persist.
- Hold both app tokens for every desktop run. Use only exact fresh
  `%TEMP%\cairn-task233-project-*` and `%TEMP%\cairn-task233-profile-*`
  identities. The owner confirms Cairn/Electron are closed immediately before
  the live run and personally enters the existing credential on the displayed
  Connect UI. After confirmed process exit, remove only those exact owned
  temporary directories and empty token locks; if identity or containment
  changes, preserve the path and STOP. Never select or send Cairn's repository
  or another owner project.
- Record only redacted non-secret evidence: exact model/provider route, request
  and response hashes, status, token usage and provider-reported cost when
  present, the local hard ceiling, display-turn identity, tests, and cleanup
  accounting. Do not record prompt, selected/proposed text, raw response,
  credential, authorization header, provider error body, or unredacted URL
  parameters. No push.

## Checks

1. **`c1` - the live transport is exact, tool-free, and spend-once.** Static,
   causal, and injected-server tests prove only the exact live Task 224 context
   plus fixed instruction can form the request; exact endpoint/model/provider/
   ZDR/no-fallback/no-collection/parameter/output caps are present; tools and
   ambient inputs are absent; one request spends before I/O; redirects,
   retries, clones, replays, substitutions, malformed responses, and alternate
   consumers fail closed.
2. **`c2` - data, credential, provider, and cost custody are bounded.** The
   disposable profile accepts only the owner-entered exact OpenRouter/Kimi K2
   connection without exposing its credential. A keyless preflight and local
   arithmetic prove the current NovitaAI route and worst-case request cost are
   under USD 0.05 immediately before sending. Logs, receipts, errors,
   provider history, phone/LAN state, and preload contain no raw prompt,
   context, provider envelope, header, or credential; renderer/transcript/
   restart carry only Task 231's exact bounded display projection.
3. **`c3` - the approved real call reaches Task 231 custody once.** With the
   owner-confirmed precondition and exact disposable identities, one real
   provider response parses against the same live context and reaches the
   existing append boundary once. Chat shows one complete inert review live;
   cold relaunch restores the same one turn without another catalog/model call,
   and selected bytes plus complete Git state remain unchanged.
4. **`c4` - refusal and effect boundaries remain dark.** Before/during/after
   selection or connection drift, price/catalog mismatch, HTTP/stream failure,
   redirect, timeout, provider refusal, tool-call output, malformed JSON,
   context mismatch, or append refusal produces no retry or effect. Ordinary
   launch, fake compatibility, provider conversation history, phone/LAN,
   action/task/composer/dispatch/result/follow-up/activation, worker/verifier,
   applier and Git-candidate routes remain unchanged and dark.
5. **`c5` - complete evidence proves the visible path and cleanup.** Focused
   Core/App causal tests, typechecks, fresh lab and production builds, bundle
   qualification, the relevant complete App suite, and a guarded dual-token
   Electron live/cold run pass. The one approved external call is accounted
   for with redacted receipt facts. Final postflight finds zero Cairn/Electron
   processes, token locks, or owned Task 233 temporary paths and no unexplained
   network request, port, file, process, credential, or cost.
6. **`c6` - records and Git isolation are truthful.** The report answers every
   check with exact commands/results, records the owner's approval and every
   transient outcome without secret or raw provider data, names all touched
   paths and limitations, appends exactly one Task 233 LOG row, and records the
   base/brief commits. Real diff/status, all worktrees, cached exact-path
   manifest, and whitespace inspection pass before one local completion
   commit; no push or unrelated path occurs.

## DONE and STOPPED

**DONE** means all six checks pass: exactly one approved, under-USD-0.05,
tool-free OpenRouter/Kimi K2 request sends only the fixed disposable Task 224
context through pinned NovitaAI; its valid response reaches Task 231 custody;
real Chat displays and cold-restores exactly one inert card; no proposal or
other effect occurs; secrets/raw custody do not persist; cleanup is exact; and
the task lands one report, one LOG row, and one isolated local commit.

**STOPPED** means no request may be sent because any preflight, identity,
connection, routing, retention, data, or price fact is not exact; or one
request was spent but did not yield a valid exact response/card/cold restore.
It also means any credential/raw data leaks, a retry or alternate provider is
attempted, an ordinary/effect route opens, selected bytes or Git state changes,
cleanup identity becomes unclear, protected work changes unexpectedly, or a
required check cannot be completed honestly. STOPPED preserves bounded
non-secret evidence, records any already-spent cost, and never retries.
