# Task 230 brief - save the resident-program visual-overhaul plan and handoff

**Lane:** A (the main checkout). **Base commit:** `c3a9575`.

## Requested visible outcome

Cairn has one durable, implementation-ready plan for the owner-approved
app-wide visual overhaul and one copy-ready handoff prompt that can start the
work in a fresh conversation without reconstructing this design session. The
plan turns the approved small resident-program Cairn, face D, slightly darker
dusty-blue and warm-paper interface, chat-first layout, and retirement of the
pond/town/worker scenery into safe serial tasks with explicit owner review
gates and executable verification.

## Boundary of intent

- This is a documentation task. Do not change renderer, Main, Core, phone,
  package, configuration, test, stored project, or generated application data.
- Do not begin the overhaul, preclaim its future task numbers, or treat this
  plan as approval to cross a later owner visual-review gate.
- Supersede only the old pond, town, cast, stone-scene, B2-face, and exact
  Lantern presentation decisions. Preserve historical records and every
  workflow-truth, authorization, provenance, project-isolation, result,
  accessibility, phone-authority, and security contract.
- Include Task 229's newly landed Builder proposal review card in the future
  surface inventory while preserving its lab-only, literal, actionless, and
  authority-free boundary.
- Preserve all existing tracked, staged, modified, and untracked work. Add
  only this task's exact plan, handoff, report, and one LOG row.

## Checks

1. **`c1` - the implementation plan is complete and serial.** It records the
   approved visual constitution, scope and exclusions, current architecture,
   dependency order, separately claimable task slices, owner gates, behavior
   preservation, test migration, risk register, and final qualification.
2. **`c2` - the handoff can safely start a fresh conversation.** It names the
   saved plan and references, requires a clean post-Task-229 preflight, tells
   the next agent what to read and preserve, prohibits risky shortcuts, and
   authorizes only the first safe phase up to its owner gate.
3. **`c3` - the documents reflect the actual settled repository.** Their paths,
   component names, Task 229 status, supported themes, compact/phone scope,
   app-token discipline, and runtime-presentation migration agree with a
   read-only audit of `c3a9575`.
4. **`c4` - records and Git isolation are exact.** Markdown/path/link checks,
   diff review, and final status show only the plan, handoff, matching report,
   and one LOG row after this separately committed brief; no product source or
   prior record changed.

## DONE and STOPPED

**DONE** means both documents are saved, internally consistent, implementation
ready, and checked against the settled post-229 repository; all four checks
pass; this report and one LOG row close the task; and exact-path commits leave
the main checkout clean.

**STOPPED** means the plan or handoff would require guessing an owner decision,
misstate the current implementation, weaken a product/safety contract, overlap
protected work, or cannot be isolated without changing product source or
historical records.
