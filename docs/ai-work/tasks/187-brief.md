# Task 187 — conversation paper

**Lane:** E

**Base commit:** `77e4701527303bf57e3fba771b70b6016dc7fd91`

## Visible outcome

The connected conversation reads as one calm editorial page inside Cairn's
lantern instead of a stack of rounded interface panels. Cairn's replies remain
plain, unboxed prose with one restrained registration mark. The owner's warm
reply becomes a flatter peach memo without a glow or inflated bubble shape.

The current proposal becomes one translucent annotated decision note. Its
heading, outcome, concern, Set aside action, Review action, and Details
disclosure belong to one composition. A concern is an indented amber margin
note with a short rule, not a second large rounded card inside the first. The
result should feel lightly printed and hand-placed, but its lines stay clean —
no scribble borders, faux handwriting, wobble, or decorative avatar changes.

## Boundary of intent

- This is a renderer presentation pass on the connected transcript and current
  proposal only. Conversation, proposal replacement, risk set-aside, Review,
  Details, keyboard, focus, scroll, queue, task, dispatch, provider, worker,
  result, and stored-data behavior stay exact.
- The proposal's heading focus target, labeled risk list, native disabled
  Review state, native Details disclosure, visible Set aside action, and all
  accessible names remain intact. Disabled Review must stay visibly present.
- Result cards, dispatch confirmation, questions, connection setup, the outer
  lantern, composer, rail, current-project annotation, pond, and cast faces are
  not redesigned here. Result evidence gets a separate information-hierarchy
  task rather than being hidden by a broad CSS cleanup.
- Use the existing pastel dusk palette and static paper grain. Add no new
  animation, dependency, asset, face path, or scribbled edge.
- No Core/CLI/phone source, dependency, credential, real provider call, worker
  call, push, publish, deployment, production system, or production data.
- Main's stopped Task 180/183 work and dormant Lane D work are protected. This
  task writes and commits only inside Lane E and does not land while main is
  dirty.

## Checks

1. Add a red-first renderer contract for flat owner/Cairn correspondence, one
   proposal folio, an amber margin concern, controlled corners, visible focus,
   and no new motion.
2. Run the focused renderer tests, the full App unit suite, typecheck, and both
   production builds.
3. With the owner's app closed and both app-token locks held, use Cairn's
   scripted local conductor fixture only to exercise the existing compact
   set-aside-to-fresh-proposal path and capture both the concern and risk-free
   states. Verify Review remains disabled/enabled at the right moments, Details
   remains operable, controls remain contained, and reduced motion is final.
4. Inspect the settled captures for one editorial page rather than nested
   rounded panels, then ask the owner for the remaining taste judgment.
5. Run `git diff --check`, inspect the exact diff and final Git status, and
   confirm the main checkout's protected work has not changed.

## DONE / STOPPED

**DONE** means the connected transcript and both proposal states visibly read
as one restrained paper conversation at compact width, every named semantic and
behavioral contract holds, every mechanical check passes, the owner confirms
the visual direction, and only isolated Task 187 paths are committed in Lane E.

**STOPPED** means the treatment still reads as nested glass/chunky cards,
becomes scribbly or overly decorative, hides a decision state, changes any
workflow/accessibility behavior, a required check cannot be repaired in scope,
protected work changes, or the owner does not confirm the visual result.
