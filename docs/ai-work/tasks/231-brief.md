# Task 231 brief - route an authenticated inert Builder review turn

**Lane:** A (the main checkout). **Base commit:** `c2885dd`.

## Requested visible outcome

One genuine, exact synthetic Task 224 context/response pair can cross a
Main-only append boundary, become Task 229's existing
`BuilderProposalReviewV1`, and appear exactly once as a distinct nonterminal
turn in Cairn's real desktop Chat. Main records external custody before the
project-writable conversation line; the ordinary conversation delta/read/
restore path carries only the closed display projection; and a cold reload
shows the same literal, actionless review.

There is no normal product control or live Builder transport for creating this
turn. A closed, positively marked E2E fixture may exercise the genuine Main
append and desktop route with fixed fake text in an identity-owned disposable
project/profile. The result still does not generate a real Builder proposal.

## Boundary of intent

- Add a distinct Builder-review role with an opaque non-action identity. Do
  not overload the `envelope` role, `ResultCard`, task actions/proposals,
  dispatch or approval state, terminal dispositions, or Task 227 reservation/
  grant machinery.
- The Main append boundary receives the exact live branded Task 224 context
  and its identity-bound branded response and calls
  `composeBuilderProposalReview` itself. Raw projections, structural clones,
  matching hashes, accessors, Proxies, wrong pairs, and byte-identical other
  contexts cannot persist anything.
- Authenticate the exact canonical project, conversation, opaque review-turn
  id, timestamp, projection digest and closed projection under a new versioned
  Main-owned external marker domain before appending project JSONL. Transcript
  or marker evidence can display only; it can never mint, reconstruct, resume,
  retry, approve, publish, verify, apply, or clean an action.
- Send only an exact-key, deeply frozen display projection or structural clone
  across IPC. Task 224 contexts, responses, brands, and selected-input custody
  never cross into the renderer.
- Render the existing `BuilderProposalReview` in the real Chat turn list with
  its exact proposal-only warning and literal React text. Add no button, link,
  form, input, callback, handler, ref, focus target, interactive role, label
  association, Markdown/HTML interpretation, dynamic tag/style/source,
  navigation, fetch, storage, popup, IPC request, or other render-time effect.
- The turn must not affect `taskBusy`, active task/action/proposal state,
  composer availability, dispatch readiness, results, terminal status,
  adapter capabilities, activation arrays, or follow-up controls.
- Deliberately omit the turn and all its selected/proposed text from conductor
  model context and phone/LAN snapshots. Do not log it to unrelated
  diagnostics. Desktop Chat is the sole product display consumer.
- Use fixed synthetic paths and text only. Do not read or persist real selected
  project files or real Builder output; call a provider/model; use a credential;
  make a paid/network call; install a dependency/browser; publish a Git
  candidate; run a command/verifier/worker; apply a file; invoke a native
  helper; widen permissions; or change phone data scope.
- Preserve Task 223's verifier/worker isolation STOP, Task 225's live-file
  application STOP, Task 228's Git durability STOP, empty activation, normal
  legacy routes, dependencies, existing stored data, protected lane history,
  and the ambiguous Task 228/229 OS-temp candidates.

## Checks

1. **`c1` - only exact live Task 224 custody can append.** Main internally
   composes from one exact branded context/response identity pair. Wrong,
   cross-context, byte-identical-but-distinct, raw, cloned, accessor-bearing,
   Proxy, or malformed inputs refuse before marker or transcript persistence;
   removing the WeakMap identity join makes the causal replay mutant fail.
2. **`c2` - external custody authenticates one exact nonterminal turn.** The
   separate versioned marker binds canonical project, conversation, opaque
   review id, timestamp, canonical projection digest and projection. Unmarked,
   forged, duplicated, tampered, truncated, extra-key, malformed, cross-project,
   cross-conversation, cross-turn and cross-timestamp lines fail closed or
   display once; marker-only crash residue displays nothing; removing the
   marker check makes the forged-JSONL mutant fail.
3. **`c3` - real desktop Chat renders complete literal inert review data.** Both
   replacement proposals and all five capability-request categories retain
   every Task 229 field and exact honesty copy. Hostile strings stay literal;
   the new role remains nonterminal and cannot change task/action/composer/
   dispatch/result state. A role-to-envelope/status mutant and every added
   action, focus, navigation, network or storage seam fail source/DOM/browser
   checks.
4. **`c4` - the existing delta/read/restore route is exact and leak-free.** A
   guarded fixed synthetic Main fixture delivers one live turn and cold reload
   restores it exactly once through the ordinary IPC/history path. Provider
   messages, phone/LAN JSON, logs, ResultCard, dispatch, activation and effect
   handlers omit it; model/phone leak mutants and unintended consumer/import
   mutants fail.
5. **`c5` - focused causal and visual evidence proves the boundaries.** Core
   focused compatibility, App unit/typecheck/store/custody/renderer tests,
   causal mutation checks, fresh production build/integration allowlist, full
   relevant App suite, and an installed-browser Electron run under the app
   token pass. The genuine Chat presentation is readable at normal and compact
   widths, contains no focusable descendant, and causes no popup, page,
   unexpected request, navigation or action. Any unavailable check is recorded
   honestly and no dependency is installed to obtain it.
6. **`c6` - records and Git isolation are exact.** The real diff, final Git
   status, named check outputs, one report answering every check, exactly one
   LOG row, cached exact-path manifest, whitespace check and one local
   completion commit are complete and truthful. No push occurs.

## DONE and STOPPED

**DONE** means all six checks pass; one authenticated synthetic nonterminal
Builder-review turn genuinely survives Main persistence and cold reload,
appears exactly once and actionlessly in real desktop Chat, and is absent from
every authority, provider, phone and effect route; exact records and one local
completion commit leave the main checkout clean.

**STOPPED** means the visible outcome does not hold, genuine Task 224 identity
custody or external turn custody must be weakened, an unintended data/effect/
terminal route opens, protected work changes unexpectedly, or completion
requires a real file, model/provider/credential/network/paid call, dependency,
approval/grant, Task 227 activation, Git candidate/ref/index/object effect,
command/verifier/worker, native/live-file write, permission change, phone scope
expansion, destructive cleanup, or unclear recovery.
