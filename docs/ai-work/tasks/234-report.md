# Task 234 report - save the Gauntlet restoration plan and handoff

**Lane:** A (the main checkout). **Base commit:** `eb46dde`.
**Brief claim commit:** `aaa1bed`.

## Outcome

Cairn now has one durable implementation plan for restoring its own modified
Gauntlet around the ordinary live route and one copy-ready fresh-conversation
handoff for the first visible slice. No application, Core, CLI, test,
configuration, package, provider, credential, stored project, generated app
data, or DELVE file changed.

The plan restores the original roles. Builder again means the existing writable
coding-worker adapter; the critic is a separate tool-free inspector; Cairn's
envelope owns checks, the one-repair limit, terminal records, commit, result
card, and commentary. Task 224's proposal-only Builder and the Q9 fake lifecycle
remain historical implementations with no right to become the production base
merely because they are extensive.

The architecture is a foreground strangler migration with six serial slices:

1. ordinary Chat pauses inside the existing serial run at one real unsealed
   candidate before terminal records or commit;
2. the compact Task Card and a Cairn-specific verification menu become
   authoritative together, so no displayed `cN` promise is merely decorative;
3. one separately approved tool-free critic may challenge only frozen checks;
4. one confirmed allegation may receive one separately approved repair inside
   the original runner and lock, followed by every original check;
5. one small Cairn self-improvement proves the actual milestone journey; and
6. post-milestone, compatibility-preserving, net-negative cleanup removes the
   shadow Q9/Builder routes from the outside inward.

The first handoff deliberately authorizes only Slice 1. It uses locally injected
conductor and worker transports at their existing seams while tests and the
owner-visible trial drive ordinary Chat IPC/Main/renderer behavior. It makes no
real model/provider call, requests no credential, imports no Q9 lifecycle, adds
no Task Card or critic, and requires the owner to accept or reject the
foreground-only recovery tradeoff before the implementation brief is claimed.

## Files touched

- `docs/ai-work/tasks/234-brief.md` - separately committed task claim.
- `docs/superpowers/plans/2026-08-14-cairn-gauntlet-restoration.md` - the
  authoritative six-slice implementation plan.
- `docs/ai-work/HANDOFF-gauntlet-restoration.md` - the copy-ready Slice 1
  handoff prompt.
- `docs/ai-work/tasks/234-report.md` - this closeout.
- `docs/ai-work/LOG.md` - one Task 234 row.

## Verification

### `c1` - the plan restores a small live Gauntlet: PASSED

The plan names one normal product spine, the restored roles and authorities,
the process-local post-worker/pre-terminal continuation, the run-lock rule for
repair, minimal Task Card and critic shapes, Cairn-specific verification, six
serial slices, owner gates, compatibility-preserving deletion order, risks,
exclusions, and the whole-plan finish line.

The first slice is no longer a display-only Task Card. It places a real visible
checkpoint inside the ordinary Chat runner. The second slice binds Task Card
promises to the worker, envelope checks, candidate, and terminal result at the
same time. That correction prevents normal Cairn from showing checks that its
legacy worker and finalizer can ignore.

### `c2` - anti-drift controls are concrete and proportionate: PASSED

The plan carries forward DELVE's useful loop - one real artifact, one plain
owner question, controlled comparisons where useful, machine checks for
technical validity, and a method change after two stops - without copying its
taste panels, exact-state proof ladders, oversized memory, or ambiguous tracks.

Its anti-drift rules are manual planning tripwires, not code or a new scoring
system. They require ordinary-route product evidence, allow at most one bounded
supporting repair after an observed blocker, prohibit a third proof ladder after
two stops, require an observed failure before new custody/qualification
machinery, keep review optional, and make cleanup explicitly claim no new
product progress.

### `c3` - the handoff safely starts one fresh conversation: PASSED

The handoff has one balanced copy-ready fenced prompt and authorizes only the
ordinary-Chat unsealed-candidate checkpoint. It requires Task 234 DONE, a clean
main checkout, full worktree/branch/task-number preflight, the app token, an
owned offscreen disposable project/profile, a brief-only claim, and one
foreground-only owner decision. It allows local fakes only at existing
conductor-transport and worker-adapter seams and requires the normal Chat
IPC/Main/renderer route.

It explicitly forbids real conductor/worker/critic/provider calls, credentials,
Task Card/verification work, critic, repair, Q9, persistence, custody systems,
sandbox qualification, dependencies, manual-route parity, and broad cleanup.
Its DONE evidence distinguishes the already-created brief from reports, LOG,
commit, result card, and `DONE`; covers continue, controlled stop, abrupt-loss
no-DONE, protected work, and the owner's one plain readability question; and
stops rather than widening scope. The owner judges a captured ordinary-route
screenshot; the prompt forbids using the owner's profile or adding a visible-
E2E marker for that gate.

### `c4` - the documents match the audited repository: PASSED

Read-only source and record checks confirmed:

- ordinary Main calls `runSerialTask(...)` in `app/src/main/tasks.ts`;
- the guarded Q9 fake calls `runSerialTaskToCandidateForStateTest(...)`;
- quality preview/activation is null/inactive on the normal route;
- the existing serial runner supports a Task-Spec authority only when supplied,
  but normal Main does not currently pass one;
- Task 220 says Q9 is fake-only and not normally activated;
- Task 221 records the normal-route/candidate gap;
- Task 224 records the role fork into a proposal-only Builder; and
- Task 233 records one HTTP 200 response refused on exact routing metadata with
  no append or retry.

The plan does not infer the actual Task 233 upstream, metadata mismatch, usage,
or charge. It replaces exact router attestation with honest router disclosure or
a direct-provider requirement when exclusive routing is an owner boundary.

### `c5` - records and Git isolation are exact: PASSED

Commands run from the Cairn repository root:

```powershell
(Select-String -LiteralPath docs/superpowers/plans/2026-08-14-cairn-gauntlet-restoration.md -Pattern '^### Slice [0-9]+').Count
(Select-String -LiteralPath docs/ai-work/HANDOFF-gauntlet-restoration.md -Pattern '^```').Count
Select-String -LiteralPath docs/superpowers/plans/2026-08-14-cairn-gauntlet-restoration.md,docs/ai-work/HANDOFF-gauntlet-restoration.md -Pattern 'TODO|TBD'
git diff --check
git status --short --branch
```

Results before closeout: six slices, two handoff fence lines, no `TODO`/`TBD`
matches, and no whitespace error. Three independent read-only audits found and
then cleared concrete issues involving display-only checks, conductor standing
authorization, crash semantics, Task Card projection, verification ownership,
same-lock repair, critic data eligibility, old saved-card compatibility,
required-reading bloat, the app token, and the two fake transport seams.

Final exact-path cached manifest, cached whitespace, row-count, diff, and status
checks are repeated immediately before the completion commit. After the brief
claim, only the plan, handoff, this report, and one LOG edit are present. DELVE
was read only. Its final status contains additional concurrent changes compared
with the initial direction-check observation; Task 234 did not create, edit,
stage, clean, reset, or commit any DELVE path.

## How to use it

1. Read
   `docs/superpowers/plans/2026-08-14-cairn-gauntlet-restoration.md` for the full
   direction and slice sequence.
2. Copy the single fenced prompt from
   `docs/ai-work/HANDOFF-gauntlet-restoration.md` into a fresh conversation when
   ready to begin Slice 1.
3. The next conversation first asks for the foreground-only recovery decision,
   then implements only the ordinary-Chat unsealed-candidate checkpoint and
   stops for the owner's one-question judgment on its captured E2E screenshot.

## Limitations and remaining owner decisions

- No Gauntlet product implementation has begun and no future task number is
  claimed.
- Foreground-only candidate state is a recommendation until the owner accepts
  it at the first handoff gate.
- Worker/provider/model/data/cost, critic mode and provider, the tiny
  self-hosting change, milestone movement, and later Q9/Builder deletion remain
  owner decisions at their real boundaries.
- No provider/model/credential use, dependency change, external call, push,
  publication, deployment, deletion, or DELVE write occurred.
- The current milestone fact did not change.

**Disposition: DONE**
