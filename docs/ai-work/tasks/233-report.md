# Task 233 report - approved live Builder call stopped at routing proof

**Lane:** A (main checkout). **Base commit:** `fab1403`. **Brief-only
commit:** `b707d11`. **Disposition:** STOPPED.

## Outcome

Cairn now has a positively guarded, Main-only live Builder transport for the
fixed Task 233 evidence route. It binds Task 232's genuine tracked-text
selection to one fixed, tool-free OpenRouter request, performs two credential-
free catalog checks, spends a durable one-call authority before network I/O,
and accepts only one exact bounded response before Task 231's existing inert
Builder-review append boundary. Ordinary launch has no entrance to this route.

The owner approved one `moonshotai/kimi-k2` call through OpenRouter with
NovitaAI pinned, sharing only the stated disposable synthetic context, using
the credential the owner personally entered in Cairn, and spending at most USD
0.05. Immediately before the run, the owner confirmed Cairn and Electron were
closed; read-only process and lock checks agreed. The test then held both app
tokens and opened one freshly owned disposable project/profile.

The owner completed Cairn's pasted-key OpenRouter/Kimi K2 connection. Both
fresh keyless preflights passed and exactly one model POST was spent. OpenRouter
returned HTTP 200 and bounded JSON, but its returned routing metadata did not
satisfy the exact direct, NovitaAI, one-endpoint, one-attempt proof. Cairn
returned `TASK233_ROUTING_EVIDENCE_REFUSED` at the response stage with two
preflight requests and one model request. It appended no Builder turn, showed
no proposal card, attempted no cold restore, and made no retry.

This is therefore honestly **STOPPED**, as the owner required. The request
asked OpenRouter to admit only Novita, disable fallbacks, deny data collection,
require a zero-data-retention endpoint, and require all parameters; however,
the response did not provide evidence strict enough to assert which upstream
actually served it. The accepted preflight arithmetic proved the bounded
request below USD 0.05, but routing refusal occurred before usage/cost evidence
was accepted. The actual charge and token counts were not retained and cannot
be asserted. No second request is authorized or attempted.

After the process exited, postflight found zero Cairn/Electron processes, zero
app-token locks, and zero Task 233 disposable project/profile paths. The
encrypted connection disappeared with the owned disposable profile. The
fresh Playwright failure-context artifact had zero matches for the defined raw
context/header/credential canaries, was hashed as
`9f9578ca2b12c22b312926174e26382a2c55c4eaf64f4eb682fce13002698f3c`,
then its exact Task 233 result directory was removed. Nothing was pushed.

## What changed

### Spend, transport, and live route

- `app/src/main/builderliveapproval.ts` adds a create-only, identity-bound
  disposable-profile spend marker. Authority is process-local, one-use, and
  consumed before any asynchronous or network boundary; its durable record
  contains only fixed non-secret hashes and limits.
- `app/src/main/builderlivetransport.ts` adds the exact two keyless GETs and one
  bounded POST. It pins endpoint, model, Novita-only routing, no fallback,
  denied data collection, ZDR, parameter support, output cap, timeout, manual
  redirect refusal, price arithmetic, response shape, router metadata, usage,
  canonical proposal-only JSON, and one-use answer custody. It exposes no
  retry, continuation, tools, plugins, browsing, actions, or ambient history.
- `app/src/main/builderlivereviewroutefixture.ts` binds Task 232's exact live
  selection to that transport and rechecks project/file/Git custody around the
  call and Task 231 append boundary.
- `app/src/main/builderproposalreviewfixture.ts` adds only Task 233's fixed
  evidence coordinates and selected request while preserving earlier fixture
  identities.
- `app/src/main/conductor/service.ts` admits only an exact current
  owner-entered pasted-key OpenRouter/Kimi K2 connection, reserves spend before
  decrypting, keeps the key in the narrow call scope, and never returns it.
- `app/src/main/main.ts` installs a one-use hook only under the complete exact
  Task 233 evidence marker and fresh direct OS-temporary identities. It keeps
  ordinary launch, updates, phone/LAN, and all other evidence markers dark.
- `app/src/main/ipc.ts` blocks OAuth and external-browser opening during the
  evidence run, leaving only the owner-operated pasted-key route.

### Evidence and configuration

- `app/tests-unit/builderlivetransport.test.ts` proves exact request formation,
  spend-before-I/O, current catalog/ZDR shape, pricing, clone/replay refusal,
  redirects, failures, wrong routing, tool calls, malformed responses, and the
  closed source-authority graph using injected fakes only. Its credential
  placeholder is deliberately non-provider-shaped so secret scanners cannot
  mistake test data for an OpenRouter key.
- `app/tests/builder-live-conversation.spec.ts` creates the disposable Git
  project/profile, passively waits for the owner's exact UI connection, invokes
  Main once, checks the inert live/cold path on success, and always validates
  and removes only its owned identities. It never locates, reads, fills, or
  drives the credential field.
- `app/playwright.builderlive.config.ts` isolates that one owner-driven spec,
  one worker, and a bounded connection/call timeout.
- `app/tests-unit/builderproposalreview.test.ts`,
  `app/tests-unit/buildertrackedtext.test.ts`, and
  `app/tests-unit/pendingboot.test.ts` update exact Task 233 allowlists while
  preserving Task 231/232 and ordinary boot boundaries.
- `app/tests-qualification/builder-proposal-bundle-dark.test.mjs` proves the
  live authority remains Main-only and absent from preload/renderer bundles.
- `app/tsconfig.json` and `app/tsconfig.unit.json` include the new Main-only
  modules and tests.
- `docs/ai-work/tasks/233-brief.md` was committed alone before implementation.
  This report and exactly one Task 233 row in `docs/ai-work/LOG.md` complete the
  stopped record.

No dependency, normal product entrance, Builder IPC, renderer control,
worker, verifier, command/tool/function-call surface, file applier, approval,
candidate/ref/index/object publication, activation, push, or deployment was
added.

## Checks

### c1 - the live transport is exact, tool-free, and spend-once: PASS

The request builder accepts only its registered exact live context object and
fixed instruction, serializes one user message, sets `max_tokens` to 1,024,
and contains no tools, plugins, web search, callbacks, response actions, or
ambient history. Exact source-consumer tests close credential, fetch, append,
and owner-UI authority to their named seams. Redirect, clone, replay, price
drift, catalog drift, malformed output, wrong route, tool-call output, and
alternate consumers refuse without retry.

The real evidence run corroborated the one-call accounting: two keyless
preflight GETs and one model POST. The spend marker was consumed before the
POST. The response refusal caused zero further external request.

### c2 - data, credential, provider, and cost custody are bounded: STOPPED at returned routing proof

The two public catalogs were read without a credential, project/profile data,
headers, or POST body. The initial implementation preflight exposed a real
schema mismatch before any paid call: the current Novita ZDR row uses endpoint
tag `novita/fp8` and permits a null maximum-prompt field. OpenRouter's public
schema confirmed that nullability; the parser was tightened to the exact live
row and all local gates were rerun. That investigation recorded only safe
catalog hashes:

- model catalog SHA-256:
  `47ce7c49a406c15a7720b17d7e57f765af678c7683d6531e492d74c764582d06`;
- ZDR catalog SHA-256:
  `c86856da2e2e746ae42d2d0208cd9ad0ce065dced83cf7fe2caa3b3325ccf361`.

The catalog prices were USD 0.57 per million prompt tokens and USD 2.30 per
million completion tokens. Exact integer/rational arithmetic, a deliberately
conservative prompt-token upper bound, and the 1,024-token completion cap
proved the immediately pre-call worst case below the fixed USD 0.05 ceiling.

The owner alone entered the key through Cairn's UI; the test did not open or
operate authentication, inspect the field, or receive the secret. No key,
authorization header, raw prompt/context, selected/proposed text, provider
envelope, or error body entered terminal output, transcript, marker, report,
phone/LAN state, preload, or renderer. The exact disposable encrypted profile
was deleted after process exit.

The response-stage failure means provider custody cannot be closed: although
the request demanded Novita-only/ZDR/no-fallback routing and the preflights
passed, the HTTP 200 response's router metadata failed at least one exact
proof. Raw metadata was intentionally discarded, so the mismatched field and
actual upstream cannot be recovered. Usage validation occurs after routing
validation; therefore actual tokens and provider-reported charge were not
accepted or retained. Those required facts remain unknown rather than being
guessed.

The routing controls and interpretations were checked against OpenRouter's
official [provider routing](https://openrouter.ai/docs/guides/routing/provider-selection),
[ZDR](https://openrouter.ai/docs/guides/features/zdr),
[router metadata](https://openrouter.ai/docs/guides/features/router-metadata),
and [usage accounting](https://openrouter.ai/docs/cookbook/administration/usage-accounting)
documentation.

### c3 - the approved real call reaches Task 231 custody once: STOPPED

One real response reached the strict response parser, but its routing evidence
was refused before usage, text, and Task 224 response acceptance. The existing
Task 231 append boundary was never called. Chat displayed zero Builder-review
cards. The harness deliberately skipped cold restore because there was no
authenticated turn to restore. The disposable selected bytes and complete Git
state stayed unchanged until the owned project was removed.

### c4 - refusal and effect boundaries remain dark: PASS

The live response mismatch produced one redacted stop code, no append, no
retry, no alternate endpoint/provider, and no follow-on request. The app then
closed normally. Ordinary launch, deterministic fake compatibility,
provider-conversation history, phone/LAN, task/action/composer/dispatch/result/
follow-up/activation, worker/verifier, applier, and Git candidate/ref/index/
object routes remain unchanged and dark. Production-bundle qualification
confirms no live authority crossed Main into preload or renderer.

### c5 - complete evidence proves the visible path and cleanup: STOPPED

Local commands and observed results:

- `app: npm.cmd run typecheck` - PASS.
- `app: .\node_modules\.bin\tsc.cmd -p tsconfig.unit.json --pretty false;
  node --test dist-unit/tests-unit/builderlivetransport.test.js` - final PASS,
  5/5 in 17.5 seconds after the last synthetic-fixture hygiene edit.
- `app: node --test dist-unit/tests-unit/builderproposalreview.test.js
  dist-unit/tests-unit/buildertrackedtext.test.js
  dist-unit/tests-unit/pendingboot.test.js` - final PASS, 23/23 in 35.7
  seconds.
- `app: npm.cmd run build:lab` - PASS, 107 modules.
- `app: npm.cmd run build:vite` - PASS, Main 114 modules, preload 1,
  renderer 79. The first sandboxed lab/Vite invocations could not access the
  already-installed local Vite/esbuild runtime; the identical commands passed
  with local execution approval and no install/update.
- `app: node --test
  tests-qualification/builder-proposal-bundle-dark.test.mjs` - final PASS,
  1/1 against the fresh bundles.
- `app: npm.cmd run test:unit` - final PASS: 886 tests, 884 passed, 0 failed,
  2 expected Windows skips, 359.5 seconds. The first final run also exited at
  its normal duration, but app output compaction lost its terminal summary;
  the same deterministic suite was rerun with only its last lines retained to
  obtain the cited exit code and counts.
- With both `%TEMP%\cairn-app-token` and `app\.app-token` held atomically:
  `app: node .\node_modules\@playwright\test\cli.js test --config
  playwright.builderlive.config.ts --workers=1 --reporter=line` - STOPPED,
  0/1. Exact terminal evidence:
  `TASK233_LIVE_STOP:TASK233_ROUTING_EVIDENCE_REFUSED:response:2:1` and
  `TASK233_PLAYWRIGHT_EXIT=1`.

The decisive visible live/cold check did not pass, so c5 and the task cannot be
DONE. Its refusal-side and cleanup claims did pass. Final postflight reported:

- `CAIRN_ELECTRON_PROCESS_COUNT=0`;
- both app-token paths absent;
- `TASK233_OWNED_TEMP_PATH_COUNT=0`;
- exact Task 233 Playwright output removed after its safe scan/hash.

No unexplained port, process, credential file, profile, project, lock, or
second cost remained. One provider charge may have been incurred for the
single request; its amount is unknown but the accepted local bound was below
USD 0.05.

### c6 - records and Git isolation are truthful: PASS for the stopped disposition

The isolated brief is commit `b707d11` on base `fab1403`. Before records, the
main worktree contained only the 16 implementation/test/config paths named in
this report and no staged path. `git worktree list --porcelain` showed main plus
lanes B, C, F, and G; read-only status checks found every lane worktree clean.
Only main contains a `233-*` record, and it contained only the committed brief
before this report. No other lane path changed.

`git diff --check` passed before records. The final exact-path cached manifest,
cached whitespace check, real diff/status, worktree state, and postflight are
inspected immediately before one local completion commit. This report answers
the failed requirements explicitly rather than claiming missing routing,
usage, cost, hashes, card, or restore evidence. Nothing is pushed.

## How to try it

Do **not** rerun the live Playwright command: its one approved call is spent,
and any new provider request requires a new exact owner approval. The safe
local regression checks are:

```powershell
cd "C:\Users\KenJL\Desktop\WebApp Projects\AI Coding Workflow Framework\app"
node --test dist-unit/tests-unit/builderlivetransport.test.js
node --test tests-qualification/builder-proposal-bundle-dark.test.mjs
```

They use injected fakes and make no provider request. A future task may inspect
the documented router-metadata contract and adjust only the proof/harness if a
strict, documented Novita route can be demonstrated. Any new live attempt must
start with a fresh brief, fresh disposable identities, fresh owner approval,
and a new spend limit; it must not treat this stopped run as resumable.

## Limitations

- No live proposal card or cold restore was produced.
- The actual serving upstream, exact metadata mismatch, token usage, provider
  charge, and actual live request/response/catalog hashes are unavailable.
  Main computed safe hashes transiently, but the failed Playwright path emitted
  only the redacted code/stage/counts before the disposable state was removed.
- The retained implementation remains an evidence-only, exact-marker route;
  it is not a normal Builder feature or authorization for another call.
- No milestone fact changed. Moving the current milestone remains the owner's
  decision after a future successfully verified end-to-end result.

Disposition: **STOPPED - the one approved response did not prove the exact
pinned routing boundary; no retry was authorized.**
