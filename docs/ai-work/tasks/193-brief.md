# Task 193 brief — paper publication checkpoint

**Lane:** E

**Base commit:** `4f36a3fcd05b6099db1860866649523e43224262`

**Synced main base:** `18a7a6e968e919783e824a7b44c1eb5daf6388bb`

## Requested visible outcome

Continue the approved visual overhaul by turning the post-DONE push nudge,
confirmation pause, and settled outcome into one cohesive paper publication
checkpoint. The nudge should read as a quiet annotation beneath the result
receipt; opening it should reveal an amber-registered, grain-soft ledger with
flat decisions rather than another rounded system card; the persistent outcome
should settle as a ruled receipt line rather than a replacement card.

The checkpoint must remain calm at compact and wide sizes, keep long remote
URLs, branch names, and commit subjects inside the lantern, show unmistakable
keyboard focus, and return focus to the restored push nudge after **Not now**.

## Boundary of intent

- This is a presentation and keyboard-continuity slice for the existing push
  flow. Preserve every target, effect, commit count/subject, public-visibility,
  recovery, progress, result, and refusal byte and their order.
- Preserve the fresh preview reread, one explicit approval, one plain push, no
  force/retry behavior, ahead-state truth, refusal classifications, IPC, Git
  arguments, persistent live-region node, and DONE-only availability.
- Keep native buttons, the focused confirmation group and accessible name, and
  the exact Push/Not now semantics. Color may reinforce but never carry state.
- Add no dependency, provider/model/worker call, browser sign-in, credential
  use, network remote, public push, production change, stored-data migration,
  project fact, milestone change, or broad redesign of cards/buttons/TaskRun.
- Do not touch the connection surface under Lane C's Task 190 planning work.
  Do not touch or land into main's protected Task 180/183 work.
- Executable push evidence may use only the existing temporary local bare Git
  remote and test fixture. It must not contact or write an external service.

## Checks

1. Add a red-first renderer contract for stable phase/state hooks, exact live
   and confirmation semantics, flat chip/checkpoint/actions/outcome, ruled
   facts and warnings, compact containment, and final reduced-motion precedence.
2. Extend the existing local-bare-remote Electron journey to prove the first
   nudge performs no push; panel focus and native tab order; exact visible risk
   facts; compact URL/control containment; decline with no write and restored
   nudge focus; one approved local push; one persistent live-region node; and
   honest settled presentation. Preserve existing refusal regressions.
3. Run the focused renderer checks, full App unit suite, typecheck, production
   Vite build, and Lab build.
4. Under both app locks with `CAIRN_TEST_LANE=1`, run only the existing fake
   conductor/offline worker and temporary local-bare-remote push journeys.
   Capture wide, compact, keyboard-focus, and settled/refused evidence as useful.
5. Inspect the final screenshots, real diff, exact-path custody, final Git
   status, and main's unchanged protected state; obtain the owner's visual
   judgment before recording completion.

## DONE / STOPPED

**DONE** means the push flow visibly belongs to the paper conversation at wide
and compact sizes; its exact risk pause and one-push truth still hold; keyboard,
focus, live-region, containment, motion, local Git, builds, and unit checks pass;
the owner accepts the result; and the implementation, report, log row, and one
isolated Lane E commit are complete.

**STOPPED** means the visible outcome or any preserved safety/accessibility/Git
behavior does not hold, the required local-only evidence cannot run safely, the
owner rejects the visual result, protected work changes unexpectedly, or the
task reaches a concrete risk boundary outside this brief.
