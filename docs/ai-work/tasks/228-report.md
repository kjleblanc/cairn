# Task 228 report - stop before disposable Git durability effects

**Lane:** A (the main checkout). **Base commit:** `edafdbc`.
**Brief claim commit:** `0ae2434`.

## Outcome

Task 228 is **STOPPED before every qualification effect**. Cairn does not have
the requested object -> private index -> private candidate ref -> authenticated
journal durability qualification because this machine has no acceptable
disposable VM power-cut facility and no qualified filesystem/durability reviewer
has been identified. Those are explicit `c4` and STOP preconditions, not details
that more local code or an ordinary process kill can substitute for.

The useful preparation is preserved. A dark, side-effect-free planner freezes
one synthetic Git-for-Windows/SHA-1/files-ref/local-NTFS matrix, computes the
exact six-object closure in memory, and emits a canonical byte-level command
plan. Its tests prove strict parsing, unambiguous canonical custody, exact
command/process expectations, closed artifact patterns, and absence from every
checked product/package entry surface. The companion plan records the not-ready
owner packet, crash taxonomy, cleanup/recovery boundary, and the exact later
approval/review requirements.

No qualification root, repository, object, private index, lock, ref, journal,
transcript, child-crash target, VM, snapshot, or virtual disk was created. No
qualification Git command was run. The planner is not an executor and cannot
invoke Git, spawn a process, write a file, or mint Task 227 authority.

## Files touched

- `docs/ai-work/tasks/228-brief.md` was created and committed alone at
  `0ae2434` to claim Task 228.
- `app/tests-qualification/git-candidate-durability-plan.ts` adds the inert
  exact-key matrix parser, raw Git-object/OID calculator, canonical plan and
  frozen command/artifact vocabulary. It imports only `node:crypto` and
  `node:util`.
- `app/tests-qualification/git-candidate-durability-plan.test.ts` adds eight
  pure tests for object identities, command custody, parser refusal,
  canonical collision resistance, supported-matrix refusal and product
  darkness.
- `app/tsconfig.qualification.json` compiles only the isolated qualification
  planner and test into ignored `app/dist-unit/qualification/**` output.
- `docs/superpowers/plans/2026-08-13-cairn-git-durability-qualification.md`
  records the reviewed preapproval protocol and explicitly marks its owner
  packet not ready.
- This report records the STOP and exact evidence.
- `docs/ai-work/LOG.md` receives one truthful Task 228 row.

Builds regenerated ignored `core/dist/**`, `core/assets/contract.md`,
`app/dist-unit/qualification/**`, `app/resources/contract.md`, and App Vite
outputs. Their tracked mirrors remained unchanged. No dependency, package
script, product route, IPC, preload, renderer, activation, profile, existing or
real-project permission/ACL, credential, provider, network, external service,
publication, push, deployment, VM, power, filesystem or operating-system setting
changed.

After the timed-out full Core compatibility command, a read-only inventory
found 357 retained synthetic `cairn-*` test-fixture directories in
`C:\Users\KenJL\AppData\Local\Temp\` with creation times inside the command's
14:43:21-14:58:30 process window: 126 `cairn-serial-test-*`, 47
`cairn-excluded-user-data-*`, and 184 other `cairn-*` directories. The command
was forcibly terminated before its terminal summary. These are not Task 228
qualification fixtures. They are temporally attributable candidate
compatibility-test residue; individual ownership, causal origin, and whether
cleanup was expected or completed were not proven. Cairn therefore did not
delete any of them. Any cleanup requires a later exact ownership inventory,
owner approval, and recovery plan.

Ordinary task bookkeeping created the isolated brief commit and will create the
local STOP commit. That ordinary Git activity is not a candidate qualification
or evidence that the planned private-ref protocol ran.

## Check results

### `c1` - the approved matrix and authority are exact: NOT RUN

The pure planner freezes the proposed matrix, fixed synthetic bytes, operation
id, private ref, six expected OIDs, object baselines, direct/no-shell process
contract and canonical plan SHA-256. It refuses accessors, proxies, malformed
UTF-16, extra/missing keys, unsupported versions/backends/filesystems, nested
fixture roots, alternate drives, ADS syntax, dot/trailing aliases, DOS devices,
console devices, and host-folder sharing.

Every command binds the absolute executable, fixture-only cwd, replaced
environment, `--no-pager`, no shell, one foreground child, 10-second timeout,
exact stdin/stdout/stderr bytes, exit code and admitted writes. The command
vocabulary explicitly uses `hash-object --no-filters`, `read-tree -i`,
`update-index --cacheinfo` without `--add`, fixed UTF-8 commit encoding,
zero-old-OID ref CAS and exact post-write object/ref reads.

This is a proposed matrix only. There is no owner-approved fixture, live Task
227 reservation/grant, VM-local binary reinspection, authenticated pre-effect
record, or execution. Therefore `c1` is not satisfied.

### `c2` - the admitted Git delta is closed: NOT RUN

The reviewed plan names exact final candidate object paths, each exact two-hex
fanout `tmp_obj_XXXXXX` family with maximum one match, the custody private index
and lock (including `write-tree` cache-tree updates), and the one loose ref and
lock. Non-writing OID cross-check and object/ref verification commands admit no
writes. Reflogs, packed refs, other object residue and every product/project
path remain forbidden by design.

No qualification Git effect ran, so no real fixture delta or protected-state
invariant was observed. `c2` is not satisfied.

### `c3` - every process-crash boundary classifies without replay: NOT RUN

The plan enumerates cuts across reservation, object, index, ref, journal,
anchor and receipt boundaries and permits only read-only restart
classification. No qualification child or crash target was spawned or
terminated, and no qualification journal/artifact prefix exists. `c3` is not
satisfied.

### `c4` - power-loss qualification is honest: FAILED AT REQUIRED PRECONDITION

Read-only host inventory found Git for Windows `2.52.0.windows.1`, Node
`v24.12.0`, and an NTFS system drive. It found no usable Hyper-V PowerShell
module, VirtualBox `VBoxManage`, VMware `vmrun`, or Windows Sandbox executable.
WSL is present but is not accepted as an externally controlled ungraceful VM
power-cut facility. No qualified filesystem/durability reviewer or exact
review scope is available.

Accordingly the owner packet remains **NOT READY**. No approval was requested,
because the packet cannot yet name the required VM/snapshot/dedicated disk,
power controller, complete cut matrix, recovery operator, bounded time/storage,
or qualified reviewer. Ordinary process termination cannot prove power-loss
ordering. This failed precondition is the decisive STOP reason.

### `c5` - the harness stays disposable and dark: PARTIAL / DARKNESS PASSED, DISPOSABLE-SAFETY NOT RUN

The planner has no filesystem, child-process, native, network, credential,
provider, package or product import. Static tests scan App `src`, `internal`,
`scripts`, package/config entry surfaces and Core `src`/package surfaces for the
planner and protocol tokens. The production Vite bundles contain no match.

No Task 228 qualification fixture was created and the final
`app/test-results/task228-*` fixture/effect residue count is zero. The separately
disclosed ignored `app/dist-unit/qualification/**` compile output exists and is
not a fixture or effect residue. Root ownership, sentinels, crash cleanup and
power-cut retention were not exercised, because they belong after exact
approval. The darkness portion passes; the disposable-safety portion was not
run.

### `c6` - compatibility, review and records are complete: PARTIAL / STOP RECORD COMPLETE

The focused pure suite passes 8/8. Core build, App production typecheck and the
production Vite build pass. Three independent read-only reviews found concrete
command, canonical, artifact, path and darkness defects during preparation,
re-read the repaired stable four-file snapshot, and returned CLEAR for the
inert preapproval boundary only.

The full Core test command exceeded its 900-second cap after Core built and a
large body of tests reported passing; it produced no terminal suite summary and
is recorded as a timeout, not a pass. Since `c1`-`c3` did not run and `c4`
failed at its mandatory required precondition, Task 228 cannot be DONE
regardless of further ordinary compatibility runtime.

## Exact commands and observed results

From the repository root, the decisive read-only host inventory used:

`$gitExe='C:\Program Files\Git\mingw64\bin\git.exe'; $nodeExe='C:\Program Files\nodejs\node.exe'; & $gitExe --version; & $nodeExe --version; Get-FileHash -Algorithm SHA256 -LiteralPath $gitExe,$nodeExe | Select-Object Path,Hash; [System.IO.DriveInfo]::GetDrives() | Where-Object { $_.Name -eq 'C:\' } | Select-Object Name,DriveType,DriveFormat; [pscustomobject]@{ HyperVModule = [bool](Get-Module -ListAvailable Hyper-V); VBoxManage = [bool](Get-Command VBoxManage -ErrorAction SilentlyContinue); Vmrun = [bool](Get-Command vmrun -ErrorAction SilentlyContinue); WindowsSandbox = [bool](Get-Command WindowsSandbox -ErrorAction SilentlyContinue); Wsl = [bool](Get-Command wsl -ErrorAction SilentlyContinue) } | Format-List`

The separate exact drive-format readback was:

`[System.IO.DriveInfo]::GetDrives() | Where-Object { $_.Name -eq 'C:\' } | Format-List Name,DriveType,DriveFormat,IsReady`

Result: exit 0. Git reported `2.52.0.windows.1` and SHA-256
`FC0F1CAE1304FCDCF4D0749F421C5ED21471EFC856301F92F56D4B844BE84363`;
Node reported `v24.12.0` and SHA-256
`2FFE3ACC0458FDDE999F50D11809BBE7C9B7EF204DCF17094E325D26ACE101D8`;
`C:\` reported fixed/ready/NTFS; Hyper-V, `VBoxManage`, `vmrun`, and Windows
Sandbox were absent; WSL was present. This was inventory only, not a
qualification Git command or durability probe.

From `app/`:

`npx.cmd tsc -p tsconfig.qualification.json --pretty false; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test dist-unit/qualification/tests-qualification/git-candidate-durability-plan.test.js`

Result on the final bytes: exit 0; 8 tests passed, 0 failed, cancelled, skipped
or todo; duration 75.5203 ms in the final evidence run.

`npm.cmd run typecheck`

Result: exit 0. The App production TypeScript configuration passed.

`npm.cmd run build:vite`

Result: the sandboxed attempt failed because esbuild could not read the Vite
config outside the sandbox. The exact approved local retry outside that sandbox
exited 0: Main built 104 modules, preload 1 module and renderer 77 modules. The
existing Vite CommonJS deprecation warnings were emitted.

`rg -n -i "git-candidate-durability-plan|cairn-git-durability-plan|task228-qualification-policy" .vite/build .vite/renderer --glob '*.js'`

Result: exit 1, no match. The production bundles contain no planner/protocol
token.

`Get-ChildItem -Force test-results -ErrorAction SilentlyContinue | Where-Object { $_.Name -like 'task228-*' }`

Result: zero entries.

From `core/`:

`npm.cmd run build`

Result: exit 0. Contract sync and TypeScript compilation passed.

`npm.cmd test`

Result: exit 124 after the 900.3-second command cap. The build passed and the
captured output contains extensive passing tests with no observed failure, but
the process was terminated before a terminal count/summary. This is not
recorded as a test pass.

The retained OS-temp compatibility residue was inventoried read-only with:

`$start=[datetime]'2026-08-13T14:43:21'; $end=[datetime]'2026-08-13T14:58:30'; $tempRoot=[System.IO.Path]::GetTempPath(); $rows=@(Get-ChildItem -LiteralPath $tempRoot -Directory -Force -ErrorAction SilentlyContinue | Where-Object { $_.Name -like 'cairn-*' -and $_.CreationTime -ge $start -and $_.CreationTime -le $end }); $groups=$rows | Group-Object { if($_.Name -like 'cairn-serial-test-*'){'cairn-serial-test'} elseif($_.Name -like 'cairn-excluded-user-data-*'){'cairn-excluded-user-data'} else {'other-cairn'} } | Sort-Object Name; Write-Output ('temp-root=' + $tempRoot); Write-Output ('count=' + $rows.Count); $groups | Select-Object Name,Count | Format-Table -AutoSize; $rows | Measure-Object CreationTime -Minimum -Maximum | Select-Object Minimum,Maximum; $rows | Select-Object -First 10 Name,CreationTime,LastWriteTime | Format-Table -AutoSize`

Result: 357 retained directories created during the process window: 126
serial-test, 47 excluded-user-data, and 184 other Cairn synthetic fixture
directories. None was deleted.

Stable SHA-256 values independently reviewed:

- planner source:
  `395BD5F2B09BFA5086268847C9BC82EBECD585628233BCB850C2ACE268DC0146`
- planner test:
  `23A4C7B66C347E8D4A6ABCD717E5B7DD3FF0DDA4356EF3A6275211C289EFF65E`
- qualification TypeScript config:
  `D1EC149456B561365995885231355F73C76BD4C649E3F80DD28D72A73EEC872B`
- qualification plan:
  `4C7ED0BD81C19E55B80E2FA81D05665E1BAA46E7C4BDCC52EDA81B8890B02394`

## Final pre-commit inspection

After exact-path staging, `git diff --cached --check` exits 0. The staged
manifest contains exactly the planner source, planner test, qualification
TypeScript config, qualification plan, this report and one LOG modification.
The unstaged and untracked path lists are empty; the Task 228 brief remains
isolated in claim commit `0ae2434`.

## How to try it

There is no qualification to try. The only safe command is the focused pure
test above; it regenerates ignored `app/dist-unit/qualification/**` and creates
no fixture or Git artifact. Do not turn the frozen commands into an executor or
run them in Cairn's repository, a real profile, the host OS, WSL, or an
unapproved temporary directory.

## Limitations and next safe choice

The preserved planner is reviewed preparation, not durability evidence, a
publisher, a live reservation, a candidate, task verification, integration,
application, Q10, or activation. Its Git facts are command-level inputs to a
future qualification; they do not jointly promise process- or power-loss
durability.

The owner has two honest future choices:

1. leave the publisher blocked and retain this STOP (the least-risk default); or
2. open a new task only after a concrete disposable VM/snapshot/dedicated NTFS
   disk and a qualified filesystem/durability reviewer are available. That task
   must complete a new exact owner packet and pause immediately before any VM,
   power, fixture, permission, Git, journal, cleanup or recovery effect.

No paid call, provider/model/credential/network access, dependency, Task 228
qualification Git effect or qualification fixture, persistent or real-project
permission/ACL change, external-service write, publication, push or deployment
occurred. The separately disclosed post-timeout inventory observed retained,
temporally attributable candidate synthetic OS-temp fixtures. The milestone did
not move.

**Disposition: STOPPED - no approved credible VM power-cut facility was
available and no qualified filesystem/durability reviewer was identified, so
Cairn preserved the inert reviewed packet and stopped before every qualification
effect.**
