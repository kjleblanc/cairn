# Task 095 — close the town-square workspace end to end

Requested outcome: The completed town-square workspace holds across real
cross-project runtime changes, accessibility and responsive paths, all existing
verification suites, and current beginner-facing usage prose.

Boundary of intent:

- This is verification and focused repair, not a new concurrency, provider,
  dispatch, storage, or authorization feature.
- Use only local fakes and isolated app data; no real provider, credential,
  paid call, external write, install, publish, or deployment.
- Preserve every task record and prior checkpoint.
- Update only documentation that became false because governed projects now
  open into the workspace and the conductor already exists.

Checks:

1. A live worker remains running and visibly badged while another project is
   selected; the viewed project shows none of its worker or task state.
2. Returning restores the worker; closing it removes the villager/thread while
   history remains.
3. Rail collapse, project order, narrow Chat/Town tabs, divider memory,
   keyboard selection, empty-ground clearing, reduced motion, and legacy paths
   remain covered.
4. Root core/CLI tests and complete desktop type, unit, Vite, and isolated
   Playwright suites pass.
5. README usage text describes the real conductor workspace and town without
   overclaiming current worker concurrency.
6. Final diff and Git status contain only this closeout task.

DONE means every approved acceptance anchor holds against the complete local
suite and the owner has accurate trial instructions.

STOPPED means a regression remains, a project leaks runtime state, or closure
would require crossing a concrete risk boundary.
