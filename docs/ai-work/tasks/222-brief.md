# Task 222 brief - add the dark production task critic route

**Lane:** A (the main checkout). **Base commit:** `64e44f4`.

**Authorization:** After Task 221 stopped before live calibration, the owner
authorized the separately reviewed local production-route prerequisite. This
does not authorize a credential, provider, network, paid, public, or external
action. The activation registry remains empty throughout this task.

## Requested visible outcome

Cairn has one complete but still-dark production route that carries the exact
owner-approved `TaskSpecV1` through a project-confined Builder, pre-seal
candidate custody, the existing bounded owner/critic/repair lifecycle, and one
honest terminal result. A guarded injected journey proves that after a future
exact activation, an ordinary task—not a Q9 fixture—can show a separately
approved packet-only critic call without widening the Builder, critic, owner,
or terminal authorities. Existing ordinary tasks remain byte-for-byte on the
legacy route while activation is empty.

## Boundary of intent

- Implement only the missing production-route prerequisite identified by Task
  221: production writer isolation, normal Evidence Plan binding, structured
  critic packet selection, a production dependency branch through durable
  pending custody, and causal offline evidence. Do not run Q10 calibration, add
  an activation tuple, tune fixtures/prompts/rubrics, begin owner-verdict Plan
  2, fetch references, add a dependency, push, publish, or deploy.
- No saved credential may be read or decrypted and no provider or network
  transport may run. Production-route tests use injected local fakes with
  impossible endpoints and no billing capability. A later calibration task
  retains every exact just-in-time owner approval.
- The Q9 branch remains exactly guarded by its Electron/test environment and
  synthetic dependency brands. Production code cannot import or rebrand Q9
  repair, critic, packet, call, or writer authority. Legacy task routing stays
  unchanged whenever no exact active identity matches the current connection.
- Only a Main/Core-owned constructor may declare a Builder candidate-capable.
  Its enforced write sandbox must be confined to the canonical project root,
  exclude Electron `userData`, ignore user configuration, disable delegation,
  and remain bound to the exact immutable adapter/run implementation. Merely
  repeating fields, supplying a process runner, renderer input, or setting an
  environment variable creates no authority.
- Main authors the revision-zero Evidence Plan before dispatch from the frozen
  Task Spec and an exact adapter event representation. Missing commands,
  non-command Cairn/owner/critic procedures, ambiguous artifacts, changed
  executable/arguments/cwd/timeout/assertion, or unqualified adapters refuse
  before the Builder. No model invents Evidence Plan authority.
- Critic selection reuses the existing tracked-text consent and exclusions:
  at most eight files, 8,000 characters each and 32,000 total; no ignored,
  untracked, linked, binary, generated, dependency, credential-like,
  outside-project, `.git`, `.cairn`, image, project-root, or `userData` bytes.
  Renderer, Builder, and project content cannot select the route or widen the
  packet.
- Production critic authority is a separate closed dependency kind. Main
  revalidates the exact non-secret connection/activation identity immediately
  before disclosure, decision, reservation, and send. Drift or an unavailable
  authoritative resolved model revision fails closed before credential use.
- Keep the one-Builder, one-repair, three-critic-call, one-unavailable-retry,
  timeout/output, owner-confirmation, completion/seal, restart, outbox, and
  terminal rules already proved by Q9. No generic IPC fallback may consume and
  discard a provider critic grant.
- Execute as four serial slices inside this one recorded task: (1) Core writer
  and Evidence Plan authority, (2) Main normal candidate admission, (3)
  production packet/critic dependency and durable decisions, and (4) guarded
  offline desktop evidence plus full regression and reviews. Each slice is
  repaired and checked before the next.
- **Desktop precondition:** immediately before guarded Electron evidence, the
  owner confirms Cairn is closed and not using the shared app profile. The lane
  acquires both app-token locks for the whole run and releases them in a
  `finally` path. The AI never closes the owner's app.

## Checks

1. **`c1` - the production Builder writer boundary is real and exact.** Only
   the built-in qualifying Codex route can mint candidate writer isolation for
   one canonical project/userData pair. Overlap, parent/sibling swaps, links or
   reparse points, hostile environment/config, Kimi/offline/custom adapters,
   injected/cloned runners, changed args, and structural support objects refuse.
   Causal process tests prove allowed project writes and denied `userData` and
   outside writes without using a provider.
2. **`c2` - one Task Spec and Evidence Plan survive dispatch unchanged.** Main
   binds every accepted `cN` to one exact pre-dispatch procedure and artifact
   set, composes Core candidate authority, and rechecks the same hashes at
   preview, approval, Builder run, capture, pending checkpoint, repair, critic,
   seal, records, and result card. Missing, ambiguous, model-authored, cloned,
   stale, or mutated authority starts no Builder.
3. **`c3` - an ordinary task uses the durable lifecycle, not the legacy close.**
   Under an exact injected active identity, normal manual and conversation
   proposals route through candidate creation and the generalized quality loop;
   Q9 fixture identity is absent. Critic off, optional decline, required clear,
   confirmed blocker plus one repair, and honest STOP each produce exactly one
   terminal result without bypassing completion/seal authority.
4. **`c4` - packet-only critic scope and route stay closed.** The production
   branch derives one canonical packet only from the frozen plan/candidate and
   Main-selected eligible tracked text. The card/body list every sent byte and
   exact provider/model/revision/fingerprint/caps/billing basis. Route,
   connection, consent, selection, request, echo, or body drift sends nothing;
   tools, images, history, credentials, project/userData paths, and undisclosed
   bytes never enter the request.
5. **`c5` - durable decisions, counters, and restart cannot replay.** Every
   Builder, repair, critic, and retry has a fresh disclosure and exact one-use
   decision; the journal records approval and reservation before any injected
   send. Awaiting approval may reattach only under the same identity; approved
   without operation, reserved, sending, unavailable, interrupted, terminal-
   prepared, and commentary/outbox cuts settle once without auto-send, cap
   reset, duplicate card/comment, duplicate report/log, or duplicate commit.
6. **`c6` - activation and legacy/Q9 separation remain fail-closed.** The
   source registry stays empty and no environment, profile, project, renderer,
   journal, or calibration record can activate it. Tests may inject an opaque
   exact active identity but cannot export or persist it. Empty or drifted
   activation keeps legacy tasks byte-identical; Q9 keeps all synthetic guards;
   calibration authority cannot enter a task and task authority cannot enter
   calibration.
7. **`c7` - complete offline regression and owner-visible evidence pass.**
   Focused red-first tests, complete Core/App suites, both typechecks, Vite and
   lab builds, exact diff/status inspection, and three independent adversarial
   reviews pass. Under the confirmed desktop precondition and both app-token
   locks, a guarded no-network normal-task matrix proves manual/conversation,
   off/optional/required, repair, restart cuts, route drift, and exactly-once
   terminal display; invocation receipts prove zero external transport.

## DONE and STOPPED

**DONE** means all seven checks pass; one production-confined Builder and one
normal Task-Spec-bound pending route exist but remain dark; the guarded injected
matrix proves the complete lifecycle without a credential/network/provider
call; existing legacy and Q9 paths remain green; one report and log row answer
every check; the exact Task 222 paths land in one local final commit; and `main`
is clean.

**STOPPED** means project-only writer isolation cannot be proved on supported
platforms; normal Evidence Plans require model-authored or vague authority;
packet selection would exceed existing consent; a production route can borrow
Q9/calibration authority; activation can be mutated at runtime; restart can
replay a call or lose work; legacy behavior changes while inactive; protected
work changes unexpectedly; the desktop precondition is not confirmed; or
recovery is unclear. Activation stays empty and Q10 calibration remains
stopped.
