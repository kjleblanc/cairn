# Task 104 report — adopt the two-lane working protocol

## What actually changed

Contract amendment (v0.3.0 → v0.4.0), its mirrors, and the protocol's two
file mechanisms:

- `CONTRACT-TEMPLATE.md` — version v0.4.0; the workflow intro now reads
  "One lane, one task, one honest result" with two lanes permitted; a new
  "Working in two lanes" section (own worktree per lane, number claimed by
  committing the brief, union-merged log, serial landing with settle check,
  single-tenant app/E2E token, lane-local DONE); the numbering paragraph
  now points one-lane projects at the next unused number and two-lane
  projects at the claim rule.
- `AGENTS.md` — the same four edits, project facts untouched.
- `core/assets/contract.md` — regenerated by the existing
  `core/scripts/sync-contract.mjs`.
- `cairn.html` — the embedded `src-contract` block replaced with the new
  canonical text (byte-exact per the mirror test) and its version eyebrow
  bumped.
- `core/package.json`, `cli/package.json`, `app/package.json` and the three
  lockfiles (`package-lock.json`, `cli/package-lock.json`,
  `app/package-lock.json`) — this project's own version fields bumped to
  0.4.0, per the changelog's one-version rule. Lockfile rewrites were
  field-exact (7 entries total); no dependency changed.
- `CHANGELOG.md` — one `0.4.0` entry describing the adoption and naming
  the spec.
- `docs/ai-work/PROJECT.md` — the working-rule line now says one serial
  task per lane, up to two lanes under the contract.
- `.gitattributes` — `docs/ai-work/LOG.md merge=union`.
- `.gitignore` — `.lanes/`.
- Lane B exists: `git worktree add .lanes/b -b lane/b` (from `45c8a35`),
  with `npm install` at the lane root (9 packages) and in `.lanes/b/app`
  (550 packages) — the dependency install the owner approved with this
  implementation; identical lockfiles; reversible by
  `git worktree remove .lanes/b`. The worktree is ignored and uncommitted
  by design.
- Records: `104-brief.md`, this report, one LOG.md row.

The parallel session's untracked work (`103-brief.md`, `105-brief.md`,
`design/`) was not staged, modified, or deleted.

## Checks run and their real results

1. **Mirror test** (`node --test core/test/contract-mirrors.test.mjs`):
   pass — template, core asset, and companion block identical. **Pass.**
2. **Root `npm test`** (core + cli): core 106/106, cli 9/9, including the
   contract-mirror test under the amended text. **Pass.**
3. **Main-tree `app` typecheck** (`tsc --noEmit`): pass; nothing written
   into the shared `app/dist`. **Pass.**
4. **Lane proof**: `git worktree list` shows `.lanes/b` on `lane/b`;
   main-tree `git status` shows no trace of it (`.lanes/` ignored);
   `npm run typecheck` inside `.lanes/b/app` passes against the lane's own
   `node_modules`. **Pass.**
5. **Isolation**: pre-commit `git status` contains only this task's named
   paths plus other sessions' untracked files, which were excluded from
   staging by exact path. **Pass.**
6. No E2E and no `build:vite` in this task, by design: the parallel
   session's Task 103 owns the app/E2E surface and `app/dist` right now.
   This task changes no behavior E2E measures — the version string feeds
   only the update-check IPC (`app/src/main/ipc.ts:155`) and no test
   asserts it. Task 103's fresh build and full suite on the merged tree
   is this landing's settle check.

Not run here but already proven before adoption (recorded in the brief):
nested worktree creation and `merge=union` both verified in a throwaway
repository — concurrent `LOG.md` appends merged with both rows kept, no
conflict.

## How to try it

1. Read the new "Working in two lanes" section in `AGENTS.md`.
2. Open a second chat in `.lanes/b` — it is a complete checkout on branch
   `lane/b` with dependencies installed. Its first task should claim its
   number by committing its brief, per the new contract.
3. Watch a future landing: concurrent LOG.md rows merge without a
   conflict.

## Limitations and remaining human judgment

- The contract now *permits* two lanes; the app token (`mkdir` lock
  directory) is a documented convention, not tooling — lanes honor it by
  following the contract, exactly as they honor the risk boundaries.
- Lane B's branch currently sits at `45c8a35`; it is fast-forwarded to
  `main` as the final step of this task (the between-tasks sync the
  protocol prescribes).
- `EVERYDAY-WORKFLOW.md`, `MAINTAINERS.md`, and `README.md` still
  describe single-lane serial work in places; refreshing that prose is
  left as a follow-up task rather than silently expanding this one's
  boundary.
- The parallel session's E2E verdict (Task 103) is still pending at
  commit time; if it surfaces a regression, that is its own finding, not
  this task's — this task touched no source the suite exercises.

Disposition: DONE — the contract names the protocol at v0.4.0, both file
mechanisms are in place, lane B exists and typechecks, and every check
listed in the brief passed.
