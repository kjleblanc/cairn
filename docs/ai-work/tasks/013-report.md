# Task 013 — conductor route spec report

## What changed

- `docs/superpowers/specs/2026-07-23-cairn-conductor-route-design.md` — new:
  the evaluation findings, the vision as a fixed reference, five owner-approved
  decisions (hosted OpenAI-compatible first body, thinking-partner first proof,
  chat as the app's new heart, standing consent at connect, conductor as a new
  layer over the kept core), the eight-phase route, the implementation rhythm,
  and the open questions deferred to Phase 1 design.
- `docs/ai-work/tasks/013-brief.md`, `docs/ai-work/tasks/013-report.md` — this
  task's records.
- `docs/ai-work/LOG.md` — one appended row.

## Checks run

- Spec self-review (placeholders, contradictions, scope, ambiguity): passed;
  deferred items are named and assigned to phases.
- Git status before writing: clean; after commit: verified clean with exactly
  one new commit containing exactly the four named paths.

## How to try it

Open the spec and read the Decisions and Route sections; each later session
starts from them.

## Limitations

This spec records direction, not implementation. The owner reviews it next and
may amend it; Phase 1 receives its own design spec before any conductor code.
The evaluation findings it cites (milestone achieved in Tasks 006 and 010,
2-of-5 real-run reliability, the fresh-checkout `npm test` failure) come from
this session's full repo read; the doc corrections they call for are Phase 0
work, deliberately not done here.

Milestone movement: NO

Disposition: DONE
