# Task 234 brief - save the Gauntlet restoration plan and handoff

**Lane:** A (the main checkout). **Base commit:** `eb46dde`.

## Requested visible outcome

Cairn has one durable, implementation-ready plan for restoring its own modified
Gauntlet around the ordinary conversation and coding-worker route, plus one
copy-ready handoff prompt that can begin the first visible slice in a fresh
conversation without reconstructing the direction review. The documents keep
the bounded candidate, critic, one-repair, verified-result loop while preventing
another fake-only qualification ladder.

## Boundary of intent

- This is a documentation task. Do not change application, Core, CLI, provider,
  package, configuration, test, stored project, or generated application data.
- Do not begin the Gauntlet restoration, run a provider or worker, request or
  inspect a credential, make a paid or external call, or preclaim any future
  implementation task number.
- Preserve the genuine protections: credentials never enter chat or records;
  paid, data-bearing, worker, repair, and external actions require their exact
  approval; Git work is protected; proposals and candidates cannot be applied
  or finalized without the authority declared by the live route.
- Treat Tasks 224-233 and Q1-Q9 as history and possible sources of small reusable
  behavior, not as architecture that must survive because it exists.
- Compare DELVE only through read-only evidence already inspected for the
  direction review. Do not edit, stage, clean, reset, or otherwise change DELVE.
- Add only this task's exact plan, handoff, report, and one LOG row after this
  separately committed brief.

## Checks

1. **`c1` - the plan restores a small live Gauntlet.** It defines the roles,
   normal product entrance, bounded flow, verification semantics, serial
   beginner-visible slices, removal order, owner gates, risks, and whole-plan
   finish line without making dark infrastructure count as product progress.
2. **`c2` - the plan contains concrete anti-drift controls.** It carries forward
   DELVE's useful visible-artifact loop, rejects its proof-ladder failures, and
   uses observable manual tripwires rather than a self-reported score, runtime
   safety harness, qualification matrix, or preventive provenance mechanism.
3. **`c3` - the handoff can safely start a fresh conversation.** It names the
   saved plan and required reading, protects current Git work and risk
   boundaries, authorizes only the first visible slice, forbids Q9/Builder
   expansion and unapproved calls, and ends with an exact beginner-visible
   result and stop conditions.
4. **`c4` - the documents match the audited repository.** They accurately state
   that normal Main uses the legacy serial route, the candidate/quality loop is
   guarded and fake-only, Task 233 stopped after one HTTP 200 response on exact
   routing proof, and no owner decision or provider fact is invented.
5. **`c5` - records and Git isolation are exact.** Markdown/path/link checks,
   diff review, and final status show only the plan, handoff, matching report,
   and one LOG row after this separately committed brief; no product source,
   DELVE path, or historical record changes.

## DONE and STOPPED

**DONE** means both documents are saved, concise enough to guide rather than
become another process system, internally consistent, implementation ready,
and checked against the clean `eb46dde` baseline; all five checks pass; this
report and one LOG row close the task; and exact-path commits leave the main
checkout clean.

**STOPPED** means the plan or handoff would require guessing a material owner
decision, authorize a paid/external or credentialed action, preserve machinery
without a product need, misstate current behavior, overlap protected work, or
cannot be isolated as documentation-only work.
