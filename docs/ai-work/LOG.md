| Task | Date | Lane | Draft/Final | Outcome | Decision | One-line summary | Milestone moved? |
|---|---|---|---|---|---|---|---|
| 001 | 2026-07-22 | Standard | Applied | DONE | completed | Formal reset: Cairn 0.0.1 with a fresh contract and docs; pre-reset history archived under docs/legacy | NO |
| 002 | 2026-07-22 | Standard | Applied | DONE | completed | Diagnosed the Codex Exec write-failure live: approval policy, ignored Windows sandbox config, and a helperless launcher stub; proved the working invocation | NO |
| 003 | 2026-07-23 | Standard | Applied | DONE | completed | Wired the proven Codex invocation into Cairn (0.0.2); a real confirmed call completed a verified task with Cairn's exact-path commit in a disposable project | NO |
| 004 | 2026-07-23 | Standard | Applied | STOPPED | stopped | Codex Exec stopped safely (ADAPTER_FAILED); requested change was not verified. | NO |
| 005 | 2026-07-23 | Standard | Applied | DONE | completed | Stopped runs now keep raw local debug evidence with precise process-failure codes (0.0.3); oversized output no longer kills a run | NO |
| 006 | 2026-07-23 | Standard | Applied | DONE | completed | Added a pre-build core test that catches drift in both canonical contract mirrors after line-ending normalization | YES |
| 008 | 2026-07-23 | Standard | Applied | STOPPED | stopped | Codex Exec stopped safely (MODEL_RECORDS_MISSING); requested change was not verified. | NO |
| 009 | 2026-07-23 | Standard | Applied | DONE | completed | Guarded the offline adapter's honest-labeling statement with a real-adapter test; completes the Task 008 offline-honesty goal | NO |
| 007 | 2026-07-23 | Standard | Applied | DONE | completed | A confirmed exact-path commit is never relabeled STOPPED by a phantom stat-dirty file; added .gitattributes (0.0.4) | NO |
| 010 | 2026-07-23 | Standard | Applied | DONE | completed | Showed the running Cairn version on task entry so stale builds are obvious | YES |
| 011 | 2026-07-23 | Standard | Applied | STOPPED | stopped | Codex Exec stopped safely (PROTECTED_WORK_CHANGED); a rerun of the Task 010 feature was poisoned by the prior uncommitted run | NO |
| 013 | 2026-07-23 | Standard | Applied | DONE | completed | Committed the conductor route spec: thinking-partner v0 in the app, core kept as the safety envelope, phased route to the vision | NO |
| 012 | 2026-07-23 | Standard | Applied | DONE | completed | A phantom stat-dirty start no longer skips the exact-path commit; real dirt is confirmed by content diff (0.0.5) | NO |
| 014 | 2026-07-23 | Standard | Applied | DONE | completed | A fresh checkout passes npm test (build now precedes the mirror test) and a new ci workflow runs the suites on every push | NO |
| 015 | 2026-07-23 | Standard | Applied | DONE | completed | Docs tell the truth: stale version prose removed for good, achieved milestone recorded, conductor destination and new milestone named | NO |
| 016 | 2026-07-23 | Standard | Applied | DONE | completed | Committed the conductor v0 design spec: voice, pushback, OpenRouter slot, layout A, constitution draft, contract amendment, eval set | NO |
| 017 | 2026-07-23 | Standard | Applied | DONE | completed | Committed the conductor v0 implementation plan: ten test-first tasks (018-027) from parser to contract amendment and 0.1.0 | NO |
| 018 | 2026-07-23 | Standard | Applied | DONE | completed | Conductor task-block parser with strict validation and the app unit-test harness | NO |
| 019 | 2026-07-23 | Standard | Applied | DONE | completed | Cairn's constitution as a versioned constant with its honesty pinned by tests | NO |
| 020 | 2026-07-23 | Standard | Applied | DONE | completed | The conductor's deterministic project briefing: facts, log, recent records, git summary, capped file tree | NO |
| 021 | 2026-07-23 | Standard | Applied | DONE | completed | Streaming OpenAI-compatible conductor client with plain-words failures and usage capture | NO |
| 022 | 2026-07-23 | Standard | Applied | DONE | completed | Project-local conversation memory under .cairn/ with a one-line gitignore guard | NO |
| 023 | 2026-07-23 | Standard | Applied | DONE | completed | Conductor keystore, standing-consent gate, and IPC surface; the key lives only in the main process | NO |
| 024 | 2026-07-23 | Standard | Applied | DONE | completed | The chat screen: the hillside fills the window and conversation floats in it, dark behind CAIRN_CONDUCTOR=1 | NO |
| 025 | 2026-07-23 | Standard | Applied | DONE | completed | The proposed-task card: concerns answered or set aside before dispatch, outcome prefills the existing task flow | NO |
| 026 | 2026-07-23 | Standard | Applied | DONE | completed | A fake body proves the whole conductor loop offline: connect, converse, card, dispatch, persistence, honest failures | NO |
| 027 | 2026-07-23 | Standard | Applied | DONE | completed | The connected conductor: contract amendment, version 0.1.0, chat becomes home | NO |
| 028 | 2026-07-23 | Standard | Applied | DONE | completed | The disclosure names the git summary, .cairn moves to .git/info/exclude so chat never dirties the tree, oversized chats fail plainly (0.1.1) | NO |
| 029 | 2026-07-24 | Standard | Applied | DONE | completed | Left the first traveler's note: the founding state, both visions, six predictions, and words for the minds who come next | NO |
| 030 | 2026-07-24 | Standard | Applied | DONE | completed | One-paste connect: curated brain picker with an honestly labeled starting recommendation and an in-app key walkthrough (0.1.2) | NO |
| 031 | 2026-07-24 | Standard | Applied | DONE | completed | Committed the Phase 2 core-surgery design spec: debts first, Cairn-authored records via a claims fence, one universal worker-result contract | NO |
| 032 | 2026-07-24 | Standard | Applied | DONE | completed | Committed the Phase 2 implementation plan: eleven test-first tasks, adversarially verified against spec and code before landing | NO |
| 033 | 2026-07-24 | Standard | Applied | DONE | completed | Watchdog timers and process-tree kill stop a wedged Codex child honestly as ADAPTER_TIMED_OUT, naming the already-spent cost | NO |
| 034 | 2026-07-24 | Standard | Applied | DONE | completed | The owner can stop a running worker through the adapter seam, closing honestly as CANCELLED_BY_OWNER with the already-spent cost named | NO |
| 035 | 2026-07-24 | Standard | Applied | DONE | completed | One task at a time now holds across processes too: a self-healing cairn-run.lock file in the git common dir, never the worktree | NO |
| 036 | 2026-07-24 | Standard | Applied | DONE | completed | Review fix: the stale-lock heal re-verifies before unlinking so it cannot delete a fresh live lock | NO |
| 037 | 2026-07-24 | Standard | Applied | DONE | completed | The owner can stop a running task from the desktop app, and quitting while one runs asks first | NO |
| 038 | 2026-07-24 | Standard | Applied | DONE | completed | Review fix: a second quit during the stop-and-quit grace can no longer skip the honest STOPPED records | NO |
| 039 | 2026-07-24 | Standard | Applied | DONE | completed | The main process now owns live run state; navigating away or reloading mid-run reattaches to the running worker and its result instead of orphaning it | NO |
| 040 | 2026-07-24 | Standard | Applied | DONE | completed | Review fix: a reattached run screen names the real worker lane instead of the offline demo copy | NO |
| 041 | 2026-07-24 | Standard | Applied | DONE | completed | The cairn-claims fence parser reads a worker's account fail-closed: one fence, seven typed keys, size-capped, nothing else survives | NO |
| 042 | 2026-07-24 | Standard | Applied | DONE | completed | Review fix: the claims fence scan is linear, so a crafted worker message cannot stall the app | NO |
| 043 | 2026-07-24 | Standard | Applied | DONE | completed | Review fix: exotic line separators are rejected fail-closed, closing a two-fence blind spot; corrects the 042 report's equivalence claim | NO |
| 044 | 2026-07-24 | Standard | Applied | DONE | completed | The worker's final agent message now rides the codex process seam into claimsText for future claims verification; the worker prompt and real-run decision logic are unchanged | NO |
| 045 | 2026-07-24 | Standard | Applied | DONE | completed | Review fix: a dropped oversized line can no longer let a stale message pose as the worker's final word | NO |
| 046 | 2026-07-24 | Standard | Applied | DONE | completed | Added core/src/records.ts: composes a worker report and LOG row that separate what Cairn verified (Git facts) from what the worker only claims | NO |
| 047 | 2026-07-24 | Standard | Applied | DONE | completed | Review fix: worker claims are blockquote-quarantined so no worker text can forge a structural record line | NO |
| 048 | 2026-07-24 | Standard | Applied | DONE | completed | The inversion: the worker drops record duties and speaks one cairn-claims fence; Cairn authors the report and log row from those claims and its own Git verification; core and app flip together | NO |
| 049 | 2026-07-24 | Standard | Applied | DONE | completed | One worker-result contract: every adapter returns one shape, throws one error family, and discloses through one seam; a synthetic third adapter reaches verified DONE with no envelope changes | NO |
| 050 | 2026-07-24 | Standard | Applied | DONE | completed | Phase 2 closes at 0.2.0: version bump across all three mirrors, an honest changelog entry for Tasks 1-10, and cli/README.md's stale model-authored-records line fixed | NO |
| 051 | 2026-07-24 | Standard | Applied | DONE | completed | Final-review fixes: a tampered brief can no longer forge DONE, an unconfirmed kill keeps the lock, POSIX kills the group, the disclosure gate follows the routed adapter | NO |
| 052 | 2026-07-24 | Standard | Applied | DONE | completed | Review fix: tampering with the log or report slot can no longer forge a DONE or a stone — Cairn restores its own records and stops honestly | NO |
| 053 | 2026-07-24 | Standard | Applied | DONE | completed | First conductor eval recorded: Kimi K2 scores 7 pass / 1 partial across the eight scenarios — the partial for an invented citation, caught and kept | NO |
| 054 | 2026-07-24 | Standard | Applied | DONE | completed | CI tells the truth (0.2.1): the root-identity gate compares real directories not spellings, the adapter-entry wait fails fast instead of spinning six hours, and both workflows carry job timeouts | NO |
| 055 | 2026-07-24 | Standard | Applied | DONE | completed | Phase 1 milestone achieved and recorded: the conductor scoped a vague request through clarifying questions and its task dispatched to a verified DONE in the Bookshelf fixture (task 004, 5b65dab); card data-fidelity finding carried to Phase 3 | YES |
| 056 | 2026-07-24 | Standard | Applied | DONE | completed | Committed the Phase 3 full-atom design spec: authorization-bound details channel, inline dispatch, envelope-authored result relay, contract-compliant push chip, constitution v2 — adversarially reviewed (24 findings) and revised before landing | NO |
| 057 | 2026-07-24 | Standard | Applied | DONE | completed | Committed the Phase 3 implementation plan: thirteen test-first tasks, adversarially verified against the code (29 findings fixed, including a details-authorization gap that all-green suites would have hidden) before landing | NO |
| 058 | 2026-07-24 | Standard | Applied | DONE | completed | Phase 3 Task 1: the catch-path record hole closes — a worker that forges a log row and then forces a thrown close can no longer leave it standing; every RECORD_VERIFICATION_FAILED throw restores the log Cairn last verified | NO |
| 059 | 2026-07-24 | Standard | Applied | DONE | completed | Review fixes on 058: the log restore is now read back and a restore that could not be written says so in the thrown message instead of being swallowed; 058's rationale was false at two throw sites and is corrected here — verified-as-written is not verified-as-true | NO |
| 060 | 2026-07-24 | Standard | Applied | DONE | completed | Phase 3 Task 2: task:run now refuses QUIT_IN_PROGRESS during the quit-grace drain instead of starting a new worker; the running-set moves out of tasks.ts into a new rungate.ts, breaking the import cycle later result-relay tasks would otherwise hit | NO |
| 061 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 3: the owner data channel opens in core — contract v2 carries details verbatim, the digest binds outcome and details together, the brief and worker prompt show them, and the codex authorization gate re-derives its card from both parts | NO |
| 062 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 4: the app-side details channel — the conductor parser accepts details fail-closed (and now rejects a newline-carrying outcome), the proposed-task card renders it verbatim, and Chat compiles against the widened onSend(outcome, details) signature | NO |
| 063 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 5: dispatch moves into the conversation — the run request is one object, details thread all four app gates, and the paid-call confirmation (six facts, one shared component) is now inline; the prefill navigation is deleted | NO |
| 064 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 6: the run lives in the conversation — a status strip carries its stage, clock, stop control and terminal line, the composer says why it is closed, and a reload reattaches; the fake-codex slow lane becomes a shared fixture | NO |
| 065 | 2026-07-25 | Standard | Applied | DONE | completed | Review fix on 064: the status strip no longer claims records for a run that wrote none — a connection-required close says what the run screen says — and its live region is one persistent element so the terminal announcement is a change, not a mount | NO |
| 066 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 7: every closed run carries composed — the report's own structured truth — with Git-derived files, verified protected-work findings, and one shared paid-call predicate so the card and the report cannot disagree | NO |
| 067 | 2026-07-25 | Standard | Applied | DONE | completed | Review fix on 066: a Git failure while composing the safety close's record no longer escapes as a raw error — it restores Cairn's own log and closes RECORD_VERIFICATION_FAILED, so no worker-forged row survives a thrown run | NO |
| 068 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 8: the envelope authors result cards — a third conversation role, posted for every terminal state, built from composed rather than from the model, with Git-derived facts separated from worker claims | NO |
| 069 | 2026-07-25 | Standard | Applied | DONE | completed | Review fix on 068: the store guard is now proved to discriminate, the claims label is exercised with real claims on screen, the conductor prompt carries the report separation instead of wrapping claims in Cairn guarantee, and the card no longer drops the worker-tamper and process-failure disclosures | NO |
| 070 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 9: the commentary turn — after the envelope posts a result card the conductor adds one short plain-language comment on it, skipped in full when disconnected, and the suite no longer risks a real paid call | NO |
| 071 | 2026-07-25 | Standard | Applied | DONE | completed | Review fix on 070: no paid comment can start while Cairn is quitting, a message the main process refused no longer sits in the transcript looking sent or points at a Stop control that is not there, and the connection fixture throws instead of deleting a real key | NO |
| 072 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 10: the push machinery — a local network-free preview and one honest `git push`, never retried or forced, classified no-remote/auth/remote-ahead/other against real bare-origin git fixtures and one call-counted injected-exec unit test | NO |
| 073 | 2026-07-25 | Standard | Applied | DONE | completed | Review fix on 072: the no-remote classification no longer conflates "no remote configured" with a real, different, remote-exists-but-untracked-branch git failure under one message that was false for the second case | NO |
| 074 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 11: the push chip and the contract's pause — a DONE card's chip only nudges, its press re-reads git and opens a confirmation naming the exact remote, branch, commit subjects, publication effect and recovery plan, and the press there is the one approval that runs one plain push; STOPPED and ERROR cards never evaluate it | NO |
| 075 | 2026-07-25 | Standard | Applied | DONE | completed | Review fixes on 074: the push is pinned to the remote and branch the panel disclosed (`push <remote> HEAD:<branch>`), so no machine's push.default can publish a branch the owner never saw; the pause now takes focus when it opens and its outcome lands in one persistent live region; the re-read no longer names a remote it can no longer check, the effect leads with the commit count, and neither failure message points at a button that is not there | NO |
| 076 | 2026-07-25 | Standard | Applied | DONE | completed | Review fixes on 075: main bounds the push target again — git accepts a URL wherever a remote name goes, so the handler now refuses any target this project has not configured as a remote, fail-closed and network-free; the refspec pins its source as well as its destination so a commit made after the panel was read cannot ride along; and the in-progress line moved into the same live region as the outcome | NO |
| 077 | 2026-07-25 | Standard | Applied | DONE | completed | Review fixes on 076: main now bounds what a push sends as well as where it goes — a caller-supplied `+<sha>` really did force a rewind and an empty head really did reach git as a branch deletion, so both refspec halves are shape-checked before git runs; refusals get their own honest `refused` kind rather than borrowing a label that says git spoke when it never ran, and the refused target is logged instead of shown | NO |
| 078 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 12: constitution v2 — three rules written from failures this project watched, so the conductor carries the owner's numbers verbatim in details, cites no source for a fact that source cannot contain, and comments on a result only from the card or the records; the push refusal now owns Cairn's narrower branch-name limit instead of blaming the project | NO |
| 079 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 Task 13 closes the phase at 0.3.0: the contract states that an envelope-dispatched run has Cairn's runtime author the records while an AI working directly under it still writes its own, names the result card, the conductor's one paid comment turn and the push button's pause, and both milestone lines stop describing the conductor-narrates model the design rejected | NO |
| 080 | 2026-07-25 | Standard | Applied | DONE | completed | Phase 3 whole-branch review fixes: a result card now has to be one Cairn wrote — authorship is vouched for by a marker outside the project, where a workspace-scoped worker cannot write — and two Git reads that could escape as raw errors with a forged or unearned DONE row standing now close through the log restore; Playwright refuses a stale bundle, the commentary test proves the card reaches the model, and a send inside the quit drain no longer starts a paid stream the process is about to kill | NO |
| 081 | 2026-07-26 | Standard | Applied | DONE | completed | The owner keeps the stone mechanism and labels the claim: the contract and the two screens that show the count now say a stone marks a task whose record CLAIMS the milestone moved, with serial.ts, the stone rule, the log header and every existing row untouched; the Playwright staleness guard covers the whole build chain (core/src to core/dist to app/.vite, since @cairn/core is bundled into main.js) and caught a real stale bundle unprompted; a byte-identical replay of a genuine result card now renders once; and the guard's own file is finally type-checked | NO |
| 082 | 2026-07-26 | Standard | Applied | DONE | completed | The eval set covers what conductor-v2 added: scenario 9 for data fidelity, scenario 10 for result commentary in the cheap offline lane, and a constitution column so a row measures a rule change | NO |
| 083 | 2026-07-26 | Standard | Applied | DONE | completed | Recorded the conductor-v2 eval: 7 pass / 3 partial for $0.03 — both new rules held, but citation honesty failed again on the very scenario it was written for | NO |
| 084 | 2026-07-26 | Standard | Applied | DONE | completed | Committed the Phase 4 second-body design: a conductor-body seam, a Claude Code body reached by detection rather than a connect flow, and a consent line that names plan limits instead of a false dollar figure | NO |
| 085 | 2026-07-26 | Standard | Applied | DONE | completed | The contract permits installing dependencies and the sandbox forbids it: recorded the contradiction, split "tools" into three categories by whether Git can undo them, kept only workspace dependencies, and placed them in Phase 5 rather than Phase 4 | NO |
| 086 | 2026-07-27 | Standard | Applied | DONE | completed | Three adversarial reviews of the draft Phase 4 plan traced five Criticals back to the spec rather than the plan: the settingSources pin was aimed at a helper instead of what query() receives, an empty tool set is tools:[] and not allowedTools:[], and the prescribed PATH shim could never protect the streaming path because the SDK spawns its own bundled binary — amended against the SDK's shipped types at 0.3.220 | NO |
| 087 | 2026-07-27 | Standard | Applied | DONE | completed | Second Phase 4 amendment, on an owner-delegated decision: the SDK ships its own Claude Code (2.1.220, via platform optionalDependencies) so an unpinned body would run a binary Cairn distributes — falsifying the consent card, making detection certify a binary that never runs, and undercutting the policy framing Decision 2 rests on; the body now pins pathToClaudeCodeExecutable to the owner's install, plus skills:[], an env allowlist, and the withdrawn tree-kill waiver since Query.close() exists | NO |
| 088 | 2026-07-27 | Standard | Applied | DONE | completed | Third Phase 4 amendment — the SDK's types do not describe its behaviour: skills:[] emits no flag and buys nothing (correcting 087's "three load-bearing pins"), strictMcpConfig and persistSession are required and were never considered, and the fake-lane guard was fail-open in the exact direction it was written to close; the plan lands with a spike as Task 0 and tasks 1-10 marked provisional with their six known defects named | NO |
| 089 | 2026-07-28 | Standard | Applied | DONE | completed | Created the owner-approved Cairn town-square workspace design brief, ready for serial implementation scoping. (worker claim; files verified against Git by Cair… | NO |
| 090 | 2026-07-27 | Standard | Applied | DONE | completed | Planned the town-square workspace as five serial implementation tasks, with real runtime identities for Cairn, workers, threads, overflow, and saved presentation state | NO |
| 091 | 2026-07-27 | Standard | Applied | DONE | completed | Conductor replies now belong to project-keyed main-process state, survive navigation, and reattach with partial text; Electron E2E tests gained an isolated userData seam | NO |
| 092 | 2026-07-27 | Standard | Applied | DONE | completed | Governed projects now open into one responsive rail, Chat, and Cairn-only town workspace with synchronized project context and preserved dashboard, run, picker, settings, and legacy paths | NO |
| 093 | 2026-07-27 | Standard | Applied | DONE | completed | The town now shows only real active-project runtime: accessible Cairn, confirmed live-worker villagers, live task threads, truthful details, safe navigation, and no idle or closed-worker overclaim | NO |
| 094 | 2026-07-27 | Standard | Applied | DONE | completed | Town entities now use a deterministic bounded force layout with drag, reset, reduced-motion behavior, and validated project-local coordinate and divider persistence under ignored `.cairn/` state | NO |
| 095 | 2026-07-27 | Standard | Applied | DONE | completed | Closed the town-square workspace against 106 Core, 9 CLI, 92 desktop unit, and 40 isolated Electron checks, including cross-project live-worker isolation, and updated beginner-facing usage prose | NO |
| 096 | 2026-07-28 | Standard | Applied | DONE | completed | Gave Cairn its owner-chosen voice — upbeat, warm, occasionally playful, plain when something is wrong or STOPPED — as conductor-v3, with every v2 honesty and boundary rule pinned byte-identical and the exclamation ban relaxed to sparing | NO |
| 098 | 2026-07-28 | Standard | Applied | STOPPED | stopped | Built the Kimi subscription seat — curated picker entry on the membership's own endpoint, plan-truth consent wording derived in main (new checkbox field joins the re-derived card), console guide — verified by 100/100 unit, typecheck, build, a new E2E, and the unmodified OpenRouter pin test; but eight pre-existing navigation/reattach E2E failures in 097's in-flight tree (twice proven independent by stashing this task's files) block the every-suite-green gate, so it stops uncommitted for the owner's call | NO |
| 097 | 2026-07-28 | Standard | Applied | DONE | completed | Put plain beginner words across the chat experience — result card, task card, dispatch confirmation, and push flow carry the same facts and the same verified/claimed separation with jargon removed ("byte-identical", "sent verbatim", "the composer" gone); 100/100 unit, typecheck, and build green, twelve E2E assertions updated to match, full suite blocked by machine-wide E2E failures proven code-independent (fails identically on the Task-095-certified commit), committed on the owner's call with the rerun owed | NO |
| 099 | 2026-07-28 | Standard | Applied | DONE | completed | Built the visual lab: a browser dev server mounts the real shipped renderer against a complete mock window.cairn bridge (no Electron, provider, credential, or Git), with a scenario panel posing quiet, thinking, running, DONE, and STOPPED states — the iteration loop for the approved "garden around you" direction; 100/100 unit, typecheck, lab bundle, and HTTP serve checks green, shipped code byte-identical | NO |
| 100 | 2026-07-28 | Standard | Applied | DONE | completed | Laid the garden's design tokens and repainted the town ground as the calm night space (deep ink, cyan and amber glows, stars, scanlines — night in both themes by design), the atmosphere layer for the port; builds and 100/100 unit green, with a disclosed mixed-tree caveat: a parallel chat's uncommitted work, including an overwrite of the committed 099 brief, sat in the tree while checks ran | NO |
| 101 | 2026-07-28 | Standard | Applied | DONE | completed | One Cairn at a time: requestSingleInstanceLock gates the whole boot — a duplicate launch quits with an empty log before any window, IPC, or profile write, the running window comes to the front, and test launches (any CAIRN_MOCK) never lock; both lanes verified on the real bundle, while two suite regressions during verification were proven external by a revert experiment amid a parallel session's active commits (renumbered from 099 after its task-number collision; suite re-confirmation at settle left open) | NO |
| 102 | 2026-07-28 | Standard | Applied | DONE | completed | Designed the two-lane working protocol for human-driven chats: brief-commit task-number claims visible across shared git refs, LOG.md merge=union, per-lane worktrees, a serial merge ritual with a settle check, a machine-wide app/E2E token, and lane-DONE verified in the lane's own tree — proposed only, no code or contract change; also repaired the overwritten 099 brief and verified the parallel session's completed Task 101 commit | NO |
| 104 | 2026-07-28 | Standard | Applied | DONE | completed | Adopted the owner-approved two-lane protocol: contract v0.4.0 (template, AGENTS.md, both mirrors, companion block) gains "Working in two lanes", LOG.md gets merge=union, lane B exists as an installed worktree at .lanes/b, versions bumped 0.3.0 to 0.4.0 across the three packages and locks; mirrors in sync, core 106/106 and cli 9/9 green, typechecks green in both trees; E2E and build:vite deliberately left to the parallel Task 103 settle run | NO |
| 105 | 2026-07-28 | Standard | Applied | DONE | completed | Designed Level 3 — the Kimi Code CLI body and worker adapter: 3a a second real TaskAdapter over `kimi -p` stream-json shaped on the Codex adapter, 3b an ACP conductor body behind the not-yet-landed Phase 4 seam, gated on a spike finding a no-shell configuration (Kimi ACP runs shell locally, unrouted); output-free authRequired detection, honest billing-floor disclosure, Codex keeps default priority, six-question owner-authorized spike precedes any implementation; design only, root tests green, no CLI installed on this machine | NO |
| 107 | 2026-07-28 | Standard | Applied | DONE | completed | Lane B's maiden run under contract v0.4.0: refreshed README, EVERYDAY-WORKFLOW, and MAINTAINERS for two lanes (per-lane serial wording, a beginner "Running a second lane" section, a "Maintainer lanes" discipline section), claimed 106 by brief commit, landed via merge with settle check green; found and documented that a fresh worktree needs one core build before the mirror test (generated asset is gitignored) | NO |
| 103 | 2026-07-28 | Standard | Applied | DONE | completed | The Task 101 settle run: full E2E re-verified 41/41 green on an isolated profile (CAIRN_TEST_USER_DATA, as fixtures were designed to read) while smoke/connect-kimi still hang on the real profile — the click-hang is data-dependent, not code: 151 accumulated test-project entries in the real profile's projects.json make main's 3.7s project:list scan outrun the renderer's 2s poll, starving both event loops (measured IPC trace; bisected across GPU, occlusion, preload, shell, caches, and profile contents); also repaired a Task 097 leftover (strip's connection-required sentence could never match its updated assertion), with the real-profile prune left as the owner's call and profile isolation + unbounded registry named as follow-ups | NO |
| 108 | 2026-07-28 | Standard | Applied | DONE | completed | Owner-approved prune from Task 103: removed all 152 test-generated Temp entries from the real profile's projects.json (byte-for-byte backup beside the original first; zero non-Temp entries existed); the real app boots responsive and the smoke + connect-kimi canaries pass on the real profile in 2.6s — the wedge Task 101 first saw is gone at the source | NO |
| 109 | 2026-07-28 | Standard | Applied | DONE | completed | E2E suite can no longer touch the owner's real profile: one worker-scoped auto fixture gives every spec file a fresh throwaway userData via the app's guarded CAIRN_TEST_USER_DATA seam (all launches spread process.env, so seven one-line import swaps cover every launch site); full suite 41/41 green with zero operator env, real projects.json identical before and after, no temp leaks, typecheck clean; stale "can't be redirected" comments corrected, snapshot fixtures kept as defense in depth | NO |
| 106 | 2026-07-28 | Standard | Applied | DONE | completed | Ran the Level 3 spike against a real Kimi Code CLI 0.29.2 (owner-approved install and OAuth sign-in, 7 small approved membership-quota calls in a temp dir): all six questions answered by observation — argv-only print prompts with a session-persistence caveat, output-free ACP auth probe (camelCase methodId; -32000 signed-out), `source=oauth` billing detection, plan mode + client-refused permissions contains shell (hazard gate passed), cancel is a notification not a request; findings amend the spec, body (3b) cleared for planning | NO |
| 110 | 2026-07-28 | Standard | Applied | DONE | completed | Bounded the remembered-projects registry at 25 entries on read (legacy oversized files are cheap immediately) and write (the file self-heals on next open), so project:list's ~25ms-per-entry synchronous scan can never again outrun the renderer's 2s poll — the Task 103 self-wedge class is closed; new E2E test seeds 30 entries and proves both caps in one open flow, full suite 42/42 green, typecheck and build clean | NO |
| 112 | 2026-07-28 | Standard | Applied | DONE | completed | Planned Level 3a (the Kimi worker adapter) as six serial tasks against the spike's observed facts: source=oauth billing-aware detection, argv-prompt exec with stream-json parse and codex-shape watchdogs, billing-selected disclosure naming the CLI's session persistence, generalized multi-adapter app wiring with a fail-closed test-lane guard (a signed-in real CLI now exists on this machine), and Decision 6's per-dispatch chooser with conductor suggestion as optional task-block fields; planning only, integration points re-read, no code changed | NO |
| 111 | 2026-07-29 | Standard | Applied | DONE | completed | Ported the approved garden character layer into the real town square: Cairn and live workers now use expressive holo faces, active workers stand on warm pads, new colors are named tokens, and focused E2E proves presence, reduced motion, and honest disappearance when the worker closes; typecheck, builds, lab serve, 100 desktop unit, focused Electron 1/1, and root 115 tests green | NO |
| 114 | 2026-07-29 | Standard | Applied | DONE | completed | Fixed the Kimi Work preview card's "Getting ready..." stall: the lab dev server now serves the real visual lab at `/` as well as `/lab/index.html`, with the entry script made absolute so imports resolve; typecheck, root/legacy HTTP checks, and lab build green while the parallel core lane stayed untouched | NO |
| 113 | 2026-07-28 | Standard | Applied | DONE | completed | Level 3a plan Task 1: core Kimi detection — three injectable probes (exit-status version, ACP authenticate with spike-corrected camelCase methodId pinned at the wire, provider-list source=oauth billing), codex-idiom resolution plus the ~/.kimi-code/bin fallback, and a fail-closed test-lane guard against the real signed-in CLI now on this machine; 115/115 core + 9/9 cli green, red-first, two disclosed test-harness repairs (nonexistent spawn cwd, unisolated home fallback) | NO |
| 115 | 2026-07-29 | Standard | Applied | DONE | completed | Level 3a plan Task 2: the core Kimi exec process — argv prompt with a 24,000-char pre-spawn guard, the stream-json parser built only from spike-observed lines (last assistant message wins, tool-call counting, documented conservative failed-tool rule, meta ignored but debugged, malformed=error, oversized dropped with finalMessage nulled), codex-shape 600s/3600s watchdogs with tree kill, cancel, force-settle, and redacted kimi-* debug copies; 130/130 core + 9/9 cli green, red-first, one disclosed harness repair (PATH stripped of System32 broke bare cmd.exe shim launches — fixed with a fake-bin+System32 test PATH, stronger isolation) | NO |
| 116 | 2026-07-29 | Standard | Applied | DONE | completed | Level 3a plan Task 3: the Kimi worker is a real TaskAdapter — billing-honest disclosure (source=oauth membership truth vs the generic floor, sessions under ~/.kimi-code/sessions/ and the CLI's static deny rules named in the data scope), codex-pattern authorization binding outcome+details+billing with the shared REAL_MODEL_CALL_NOT_AUTHORIZED code, the worker prompt (print-mode honesty line, subagents/background tasks forbidden, no apply_patch), priority-90 factory translating to worker-result/v1 (completed iff exit 0 + clean stream + final message), and routeTask pins (codex-100 sorts first, override to kimi honored); 137/137 core + 9/9 cli green, red-first, fakes only | NO |
| 117 | 2026-07-29 | Standard | Applied | DONE | completed | Added a lab-only avatar concept board: Lopsided hologram, Grown-up Ed, and Interface spirit shown side by side across ready, thinking, working, DONE, and STOPPED poses, linked from the visual lab and bundled without touching shipped renderer code; typecheck, lab build, all three HTTP paths, and an isolated Electron render/screenshot check green | NO |
| 118 | 2026-07-29 | Standard | Applied | DONE | completed | Added Concept 04, Warm interface spirit, to the lab-only avatar board: Concept 03's sparse digital field and waveform warmed by Concept 01's asymmetric eye and off-center mouth, shown across ready, thinking, working, DONE, and STOPPED beside the original three; typecheck, lab build, all HTTP paths, and isolated Electron render/screenshot green, with shipped renderer code untouched | NO |
| 120 | 2026-07-29 | Standard | Applied | DONE | completed | Ported the owner-approved Concept 04 (Warm interface spirit) into the shipped town square: Cairn's live avatar now renders the dashed spirit field, asymmetric stroke eyes, and warm cyan-amber waveform mouth across ready/thinking/working, workers wear the same spirit working pose in amber, and the blush-and-smile emoji face is gone; typecheck, 105/105 unit, lab build, focused town E2E 1/1, and an isolated Electron render with ready/thinking/working screenshots all green, behavior and E2E-relied wrapper classes unchanged | NO |
| 119 | 2026-07-29 | Standard | Applied | DONE | completed | Level 3a plan Task 4: the app wires every connected adapter — detection generalizes into pure app/src/main/adapters.ts (codex prose byte-identical, both CLIs named when both are probed), the fake-kimi lane completes DONE through the real-call path with cross-adapter confirmation refusals, and serial.ts stop records brand from the routed adapter so a kimi timeout or boundary stop never claims OpenAI; 139/139 core, 9/9 cli, 105/105 unit, 43/43 E2E with the app token held, red-first, fakes only, one measured fixture disclosure (.cmd shims truncate multi-line argv; the real native CLI does not) | NO |
| 121 | 2026-07-29 | Standard | Applied | DONE | completed | Took the circles off the town faces: orbit ring, holo disc, and inner field ring removed so the warm spirit face floats as bare glowing marks (worker floor pad kept — a floor, not a face frame); typecheck, 105/105 unit, lab build, focused town E2E 1/1, and isolated Electron render/screenshots green | NO |
| 122 | 2026-07-29 | Standard | Applied | DONE | completed | Built the unification look board: one lab-only page painting the same town scene (bare-marks Cairn, worker on pad, thread, hills, card, button) under three directions — Vibrant garden (GitS palette on the characters), Soft festival (Animal Crossing dusk), Deep calm (de-digitized today) — with swatches, motion notes, and keep/watch lines for the owner to judge; typecheck, lab build, all four HTTP paths, and isolated Electron render/screenshot green; shipped code untouched | NO |
| 123 | 2026-07-29 | Standard | Applied | DONE | completed | Ported the owner-chosen Soft festival palette into tokens.css only (the other lane holds app.css uncommitted): dusk lavender garden and chrome, coral Cairn, lantern-amber worker and highlights, warm firefly stars, cream-on-lavender town text, scanlines and the perspective grid retired to transparent through their tokens; typecheck, 106/106 unit, lab build, focused town E2E 1/1, and isolated Electron render/screenshots green, with the parallel lane’s files never touched | NO |
| 124 | 2026-07-29 | Standard | Applied | DONE | completed | Pinned Kimi K3 as the recommended brain (id and ~5× price re-verified against OpenRouter's keyless catalog; dimes blurb, K2's cents untouched), gave every picker entry a plain billing line, added the open "model isn't listed" panel with the exact add-a-model sentence and copy button, and made the Kimi seat's guide tell the CLI-sign-in truth; 106/106 unit (billing pin red-first), typecheck, both builds, 43/43 E2E with the app token held, one in-task repair (picker copy broke the suite's Custom… substring locator — copy was the bug), screenshots inspected; renumbered from 123 after the palette lane's later same-number claim landed first | NO |
| 125 | 2026-07-29 | Standard | Applied | DONE | completed | One sky: the workspace is now one continuous Soft festival dusk — the shell carries the firefly sky behind everything, the rail is a frosted scrim floating on it (opaque slab and page-edge shadow gone), the chat pane and embedded chat are transparent so the conversation floats, the divider is invisible until hovered, the town shares the shell sky instead of painting its own, and the dead grid element and tokens are removed; typecheck, 106/106 unit, lab build, focused town E2E 1/1, and isolated Electron render/screenshots green | NO |
| 126 | 2026-07-29 | Standard | Applied | DONE | completed | Streamlined the connect flow: the card now opens by asking "How do you want to power Cairn?" with the two primary doors (K3 recommended, Kimi subscription) rendered from the same Body data, and the picker collapses K2/DeepSeek/GPT-5 Mini behind a "More choices" toggle while Custom… and the not-listed path stay visible; consent surface byte-identical, connectToFixture's 22 calls untouched; 107/107 unit (primary pin red-first), typecheck, both builds, 43/43 E2E with the app token held across a Daimon restart, screenshots inspected; survived a second double-claim — the parallel lane's later 126 brief (motion slice) owes the renumber to 127 | NO |
| 128 | 2026-07-29 | Standard | Applied | DONE | completed | Motion slice, Animal Crossing softness, delivered as a new motion.css (the other lane held app.css at task start): softer --spring, chat-arrive entrances for bubbles/cards/strip, town-node arrive pop, a one-time face pop on every Cairn state change, calmer 7.5s bob and breathing skyglow, 460ms node settle, pointer-responsive named buttons, and a cascade-aware reduced-motion block that re-kills all of it; typecheck, 107/107 unit, lab build, focused town E2E 1/1 with position and reduced-motion assertions, and isolated Electron render with CDP-emulated reduced-motion probes green; renumbered from 126 after the double-claim (landing commit 8b895f8 keeps the old number in its message), with two repairs: the other lane's 126-report restored from 2698644, and a renderer/bodies.ts re-export shim fixing the HEAD breakage from their staged rename sweeping into 8b895f8 | NO |
| 129 | 2026-07-29 | Standard | Applied | DONE | completed | Pinned the visual lab's dev server to its own default port 7410 in vite.lab.config.ts (was Vite's 5173), strictPort left false and CLI --port still overriding so the Kimi Work preview card is unaffected; verified live (server reported localhost:7410, lab root HTTP 200, server stopped and port freed), typecheck and lab build green; only the lab config plus records committed | NO |
| 127 | 2026-07-29 | Standard | Applied | DONE | completed | Option D: the card remembers the last seat ({baseUrl, model} in profile-local storage, never the key — reconnect is one pre-filled paste with a "never your key" line), and a custom seat now earns one code-assembled note in the conductor's prompt (model id + host only, both already visible to the provider) offering the add-a-model task once; bodies.ts moved to shared/ as one source of truth; 111/111 unit (seatnote red-first), typecheck, both builds, 44/44 E2E with the app token held (new wire test reads what the provider was sent; connect test given an explicit fresh-card reset after the memory broke its implicit precondition), screenshot inspected; landed amid the motion lane's landing-without-renumber and its verified-pending repair | NO |
| 130 | 2026-07-29 | Standard | Applied | DONE | completed | Overlays, not pages: the shell is now a base view plus an overlay layer — settings and the in-project picker rise as cards (overlay-rise on the town spring, scrim fade) over the dimmed, still-mounted world, closing three ways (×, scrim, Escape) with the base inert+aria-hidden behind; Workspace keyed dir:session so overlay-opened projects can't inherit internal switch state; dead Task 128 bodies shim deleted (zero importers after 127); typecheck, unit, both builds, 45/45 E2E per-spec with the app token held, focused isolated-Electron checks (town mounted under overlay, animation-names incl. reduced-motion none) and screenshots green; two disclosed harness repairs (role→DOM locator, animation-finish before capture) | NO |
| 131 | 2026-07-29 | Standard | Applied | DONE | completed | Sign in with OpenRouter — the one-click door: checkbox-gated button on any OpenRouter seat starts a PKCE dance entirely in main (loopback listener, S256, exchange), the minted key goes straight into the existing encrypted keystore via the same saveKey as paste and never crosses IPC; the consent gate is byte-identical (doctored card/unchecked box refused) with a fail-closed OpenRouter-only pin, paste stays as the universal fallback, waiting panel has a fallback link and silent Cancel; 123/123 unit (oauth red-first), typecheck, both builds, 47/47 E2E with the app token held AFTER task 130 landed mid-task — the fixture itself verifies verifier↔challenge end-to-end; cancel assertion corrected to the honest connection-refused; one disclosed pre-existing busy-text flake, green in reruns; screenshots inspected; the real OpenRouter handshake is still owed its live confirmation by the owner's first sign-in | NO |
| 132 | 2026-07-29 | Standard | Applied | DONE | completed | Drafted the seven-project contract-evolution proposal: eight candidate v0.5.0 changes (decision partition, milestone records-rule, checkable-evidence rule, Core/Verified/Forensic evidence levels, version-drift rules + ARCHIVED status, portfolio section, measurement preconditions, human-judgment stops), nine rejected ideas, and a per-project rollout table; contract files untouched pending owner decision; landing deferred until lane As task 131 closes | NO |
| 133 | 2026-07-29 | Standard | Applied | DONE | completed | Applied the owner-approved Task 132 proposal as contract v0.5.0 across AGENTS.md, CONTRACT-TEMPLATE.md, the cairn.html embed, the three package files and locks, MAINTAINERS.md, and PROJECT.md (declares Verified), with ARCHIVED status, decision partition, evidence levels, template-drift rules, milestone records-rule, checkable-evidence reports, measurement preconditions, and human-judgment stops; CHANGELOG 0.5.0 entry; docs-review/staged/ gitignored; 111/111 app unit and 139/139 core green after in-worktree npm install and core build; no runtime behavior changed | NO |
| 134 | 2026-07-30 | Standard | Applied | DONE | completed | Shots page: the lab gains a Review shots viewer (lab/shots.html, plain static HTML matching its siblings) served at the short /shots.html by the lab middleware and added to build:lab inputs, reading untracked generated content from gitignored app/shots/ (manifest + PNGs); first entry is Task 130's two settled overlay screenshots, establishing the owner's screenshot-first review routine after chat image-gallery cards failed twice (raw code blocks); typecheck, build:lab, and live curl of page/manifest/both images green, rendered-page capture inspected; three disclosed harness repairs (no bundled Chromium → throwaway Electron capture app, which needed its own package.json to exit, and the /lab/ URL-space miss that first rendered the app mock); server stopped, temp files deleted | NO |
| 135 | 2026-07-30 | Standard | Applied | DONE | completed | Moved the lab's default port from 7410 to 7640 in vite.lab.config.ts (strictPort and CLI override unchanged); survey found two Kimi Work preview servers already holding 7640/7641 — they follow the config default with no CLI port and hot-reloaded the edit, so 7640 now serves the lab including /shots.html; typecheck, build:lab, and curl of root/shots/manifest on 7640 green, my temporary verification instance stopped, Kimi's preview processes deliberately left running | NO |
| 137 | 2026-07-30 | Standard | Applied | DONE | completed | The quiet card: connect card compacted to three lean flavors — OpenRouter seats show name -> consent -> checkbox -> Sign in with OpenRouter + hint + "Use a key instead" toggle opening three inline steps (openrouter.ai/keys pill, copy, paste); Kimi seat shows the three steps as the screen with console pill and short CLI-truth line; Custom unchanged; guide panel and its two list screens removed along with intro/"Connecting with X" lines; billing lines now "Bills per use — sign in or paste a key"; consent.ts byte-identical and fully visible in every flavor; 124/124 unit (red-first billing pin), typecheck, both builds, 47/47 E2E with the app token held; two disclosed test-side flake repairs (atomic chip locator; deterministic commentary hold/release gate in the fixture replacing a delay hack, root cause proven by snapshot); three screenshots inspected; taste confirmation invited | NO |
| 138 | 2026-07-30 | Standard | Applied | DONE | completed | Drafted the N-lane protocol spec from the two-week pilot evidence (four double-claims, union merge perfect, app token and worktrees held): keep claim-by-commit with one hardening sentence, N worktrees with an onboarding note, three-lane default cap, landing-queue etiquette (main must be between tasks), a lane defined as a conversation not a device, and automations-are-not-lanes; renumbered from 134 after restoring the shots-page brief I had overwritten | NO |
| 142 | 2026-07-30 | Standard | Applied | DONE | completed | Drafted the mobile groundwork spec (renumbered from 139 after lane A's later 139 claim landed first) per the owners locked decisions (LAN v1, full approval parity, bottleneck is proximity): one no-framework HTTP/WebSocket seam in the existing main process over the untouched conductor service, code pairing with a revocable device list, whole-object approval parity through the same four gates, a web build of the chat surface, QR discovery, plain-HTTP-on-Wi-Fi disclosed with encryption deferred to the overlay phase, and a phone-to-DONE milestone; secrets never leave the PC | NO |
| 136 | 2026-07-30 | Standard | Applied | DONE | completed | Chat-in-scene mockup: a lab-only page rendering the real TownSquare against the mock bridge with three prototype conversation treatments over it — A tailed dialog anchored to Cairn (tucks to a chip), B full-width rising drawer (tucks to a slim bar), C left side panel over a dimmed scene (tucks to an edge tab) — switchable via dashed-amber lab chrome, with six settled captures (open/tucked each) added as the top entry on the Review shots page; typecheck, build:lab (new chatmock input), and curl of page/manifest/images green, my dev server stopped; three disclosed harness repairs (import-hoisting blank mounts fixed by boot/view split, vanishing .tmp-shot plus silent electron.exe exits worked around with the proven Playwright-driver capture, heredoc regex mangling) and one visual repair found by inspection (C's panel opening under the chrome) | NO |
| 139 | 2026-07-31 | Standard | Applied | DONE | completed | Moved the lab's default port from 7640 to 7390 in vite.lab.config.ts (strictPort and CLI override unchanged, preview card unaffected); typecheck, build:lab, and live curl of root/shots/chatmock on 7390 green; disclosed harness note — an orphaned Vite child survived the first stop and was killed by PID, port confirmed free, Kimi Work's two preview servers untouched | NO |
| 140 | 2026-07-31 | Standard | Applied | DONE | completed | Amended the contract to v0.6.0 "Working in lanes" per the owner-delegated Task 138 decisions: lane = conversation not device, three-lane default cap, claim hardening in contract text (a number is taken if its brief file exists), landing etiquette (main must be between tasks), profile-not-device tenancy, automations-are-not-lanes, worktree onboarding note; mirrors, MAINTAINERS, spec status, and 0.6.0 package versions updated; 139/139 core and 124/124 app unit green; no runtime behavior changed | NO |
| 141 | 2026-07-31 | Standard | Applied | DONE | completed | Adopted the mobile groundwork: spec status accepted with the four delegated answers (scope bend accepted, plain-HTTP-with-disclosure v1, fixed printable port, phone is a lane only when working the repo), PROJECT.md scope line rewritten with the single dated LAN-listener bend and the route naming the phone-to-DONE milestone; no version bump, no code, one disclosed edit repair | NO |
| 144 | 2026-07-31 | Standard | Applied | DONE | completed | World palette board: new lab-only page painting one real TownFace scene under three de-digitized directions per the owner's "Animal Crossing with Ghost in the Shell warmth" note — Meadow morning (full AC daylight, cream panels, chalky colors), Golden hour (middle step), Lantern dusk (today's night de-neoned) — with keep/watch notes and chips per direction; three settled captures added as the top entry on the Review shots page; typecheck, build:lab (new worldboard input), and curl of page/manifest/images green, my dev server stopped; disclosed repairs — renumbered 140 to 144 after lane B's prior 140 claim (their brief restored byte-for-byte), mock-cairn given type-only imports for lane B's in-flight ipc.ts instead of duplicate mock methods, hidden-window capture stall solved with a briefly visible window; lane B's uncommitted work untouched; winner gets ported into tokens.css as a later task | NO |
| 143 | 2026-07-31 | Standard | Applied | DONE | completed | Hello, phone (pair and read): the app hosts one LAN HTTP listener (no new dependencies) translating onto the untouched conductor service — desktop Settings shows the address + a 5-minute single-use code + the spec's disclosure sentence, the phone exchanges it for a hashed-at-rest device token (revocable, sessions cut at once, one fixed refusal for unknown/revoked, fail closed everywhere), and a self-contained read-only phone page live-updates over SSE (the owner's delegated choice over hand-rolled WS); 17 new unit pins (pairing accept/refuse/expiry/single-use/burn, revocation mid-stream, port walk, snapshot carries no paths/URLs), 141/141 unit, typecheck, 139/139 core + 9/9 cli, 48/48 E2E with the app token held incl. a new spec driving the whole flow in a real second window (one conductor timing flake green in isolation); lane C's 144 landed mid-task over this lane's dirty tree and absorbed the uncommitted mock-cairn edit — main completes when this commit adds the shared types; owner's physical phone is the named final confirmation | NO |
| 145 | 2026-07-31 | Standard | Applied | DONE | completed | Ported the owner-picked Lantern Dusk palette from the Task 144 board into the real app: tokens.css dark side and always-night garden now read plum dusk (#2e2a4e/#2c2842) with cream lamp-lit panels (#f4ead9/#fffaf0) and warm dark ink (#54452f), Cairn coral flipped to soft teal #7fd8c8, hearth amber de-neoned to lantern gold #f2b95c, serious moments coral #ff9e8a, cyan glows (rail mark, thread chip) removed; two declared additions (--card-ink/--card-muted) carry text on cream surfaces, town node labels keep light ink on the open ground; phonepage.ts's embedded token mirror harmonized; app.css surface rules updated, no layout/behavior changes, light theme untouched, green family kept; typecheck, 141/141 unit, build:vite and build:lab green (brief's "npm run build" was a typo for build:vite, disclosed); three inspected captures (workspace, chat, town — including a native-res header crop) published as the top shots-page entry | NO |
| 146 | 2026-07-31 | Standard | Applied | DONE | completed | The villager bubble, for real (owner's Task 136 pick, treatment A): the split chat pane is gone — Workspace.tsx rewritten so the town fills the stage and hosts the conversation; Chat.tsx embedded mode now renders a cream tailed dialog anchored to Cairn's node at mock geometry (role="dialog", villager-rise entrance) that tucks into a floating chip (villager-bob) and untucks via the chip or via a new focusSignal from clicking Cairn's node; divider, narrow tabs, chat pane, and --chat-width removed, dividerWidth kept in defaultTownPresentation for saved-file compat; dashboard/run views take the full stage; standalone chat untouched; app.css villager styles added with reduced-motion and 620px fallbacks, dead layout rules removed; typecheck, 141/141 unit, build:vite and build:lab green; 48/48 E2E with the app token held (chunked per-file over the 300s cap) incl. projects.spec updated for the deliberate design change with a disclosed force-click on the bobbing chip; two earlier line-122 boot flakes proven environmental (registry full at launch x3, standalone repro 2/2); token taken at app/.app-token not the $TEMP convention, disclosed; three inspected captures (bubble, tucked chip, full-stage home) published as the top shots-page entry | NO |
| 147 | 2026-07-31 | Standard | Applied | DONE | completed | The cast board: a lab-only design page for the owner's avatar idea — five hand-drawn SVG villagers grounded in the real adapter roster (Cairn as a stone-stack spirit keeping the real face marks + paper lantern, Mochi the moon rabbit for Kimi, Rusty the fox for Codex, Barnaby the owl for Claude, Pip & Kit the twin kittens for Gemini), each with exactly one Ghost in the Shell detail (glow seam / ear line / tail wisp / wing circuit / halo ring), shown under three lighting directions reusing the Task 144 worlds (Paper villagers at Meadow morning glow 35%, Golden hour 65%, Neon seam at Lantern dusk glow full with CSS rim-light) with essence/keep/watch notes, a 44px town-size strip proving silhouettes survive, and placeholder villager names flagged as the owner's call; typecheck and build:lab clean (new castboard input), page/manifest/images 200, five inspected captures published as the top shots-page entry; disclosed repair — capture harness switched to channel:"chrome" (no Playwright browser cache on this machine; E2E drives Electron); lab-only, shipped app untouched, winner ports as a later task | NO |
| 148 | 2026-07-31 | Standard | Applied | STOPPED | stopped | Codex Exec stopped safely (RECORD_VERIFICATION_FAILED); requested change was not verified. | NO |
| 149 | 2026-07-31 | Standard | Applied | DONE | completed | The cast, take two (owner's pivot on 147: keep the stroke-face style, personalize per model): lab cast board reworked into a face gallery in the app's exact TownFace vocabulary — Cairn keeps the real marks byte-for-byte (soft teal), Kimi rests in closed happy arcs (lilac), Codex winks with a thinking-mark angle + smirk (amber), Claude gazes level (dusty blue), Gemini doubles every stroke (mint), with today's shared coral face shown as the before; per-model cards (large + 40px town-size chips on dusk and light, hex, rationale) plus line-ups in the shipped Lantern dusk and Meadow morning daylight check; typecheck and build:lab clean, page/manifest/images 200, three inspected captures published as the top shots-page entry (manifest renumbered, image filenames keep task-148- prefix); RENUMBERED from 148 after the envelope's stopped Codex Exec run claimed 148 concurrently (its retained Picker/spec edits, 148-report, and STOPPED LOG row left uncommitted for the owner's decision); LOG.md itself left uncommitted this task because committing it would sweep the worker's row into history prematurely; lab-only, shipped app untouched | NO |
| 150 | 2026-07-31 | Standard | Applied | STOPPED | stopped | Codex Exec stopped safely (MODEL_REPORTED_STOPPED); requested change was not verified. | NO |
| 151 | 2026-07-31 | Standard | Applied | DONE | completed | Made the villager bubble usable after the owner's jumbled-mess screenshot: dialog now min(520px, 50%-128px) wide with a 400px floor and clearance-preserving clamp off Cairn's node (slides over him only below the floor, the accepted narrow-pane tradeoff), max-height 76%; only the message list shrinks so it keeps a real scroll region, with a styled thin scrollbar replacing the OS arrow-button bar (the one shared standalone-chat touch); run strip wraps with the outcome on its own row so both controls stay visible; top bar fits one line via a zero-basis ellipsizing provider-model pill; route-facts 1fr 1fr and mono paths wrap; chip clamped; typecheck, 141/141 unit, build:vite + build:lab green; four inspected real-app fixture captures (running/settled/narrow/tucked) published top of the shots page; conductor.spec 26/26 (caught the widened bubble covering Cairn's node — fixed) + bridge.spec 1/1 with the app token held, projects.spec deliberately not run (stopped worker's file); disclosed repairs — pill basis, node clearance, stale-bundle recapture, environmental worker crash and two 300s timeouts with orphaned electron.exe killed by PID; LOG.md left uncommitted per the Task 149 precedent; Task 150's records landed mid-investigation as its run settled | NO |
| 152 | 2026-07-31 | Standard | Applied | DONE | completed | The cast learns to feel (owner: "build them all" on the five expression options): lab cast board reworked with five states per face (Cairn's ready/thinking/working byte-for-byte the app's, only its done new; Kimi's arcs flatten/narrow/open, Codex's wink opens + goes keen + grins, Claude's strokes dip/shorten/warm, Gemini's pairs rest alternately + arc both) plus a distinct delighted geometry each; signature marks (Kimi crescent, Codex spark, Claude brow-line, Gemini bobbing dots — dots are zero-length strokes, vocabulary holds); tilt (Codex +4, Kimi -2, Gemini -1.5, Claude/Cairn level); per-face blink rhythms live via CSS on nested eye groups (single/double-left-only/slow/alternating/squeeze, reduced-motion safe); reactive one-shots driven by real board buttons (delighted flash 1.6s, waiting glance 2.5px via nested fb-look group); state grid with today's shared coral face as the before row; typecheck and build:lab clean, page/manifest/images 200, four inspected captures (grid/live/delighted/waiting — reactions triggered through the real buttons) published top of shots page; disclosed — own tmp-faceshot harness (chrome channel, deleted after) since app/tmp-capture is another actor's; LOG.md row appended but file stays uncommitted per 149/151 precedent (pending 148-151 rows await the owner); lab-only, shipped app and foreign uncommitted evidence untouched | NO |
| 153 | 2026-07-31 | Standard | Applied | DONE | completed | Fixed the owner-reported second-dispatch wedge: the envelope's result-card comment streamed INVISIBLY (composer looked ready, every send refused, refused text cleared from the composer) and the spent proposal card lingered with a still-clickable Send to dispatch; ConductorDelta gains turnKind, main emits a quiet message-less error event when a comment fails/aborts/overflows so the renderer releases, Chat.tsx shows the comment as a labeled bubble with no Stop (main's deliberate design) while the composer stays enabled (Task 070), refusals now restore the composer text, and startDispatch clears the stale card; conductor.spec gains the owner's exact regression (second proposal gets its enabled Send to dispatch, second run completes) plus tightened settled-turn selectors (:not(.bubble-commentary)) and refused-text/caption pins in the Task 070 test; RENUMBERED from 152 after lane B claimed 152 mid-task (their brief restored byte-exact, per contract); typecheck, 141/141 unit, build:vite + build:lab green; 8/8 fixture-harness assertions and two inspected captures published top of the shots page (they also show the shipped 151 geometry, 408px at the default window, vs the 151 shots' pre-repair 520px — disclosed); conductor 27/27, bridge+away+serial 4/4, routing+smoke+connect-kimi 14/14 with the app token held, projects.spec not run (protected); LOG.md uncommitted per the Task 149 precedent | NO |
| 154 | 2026-07-31 | Standard | Applied | DONE | completed | E2E runs no longer touch the owner's screen (owner: "whenever we run tests, it minimizes and interrupts what's on my screen"): test windows park off every display — position computed from screen.getAllDisplays (+2000px past the farthest edge), focusable:false, skipTaskbar:true, backgroundThrottling:false — gated on CAIRN_E2E=1 so no ordinary launch can be parked; bridge.spec's phone window parked the same way inside app.evaluate; show:false was tried first and rejected WITH evidence (hidden pages got 3 rAF/2s, Playwright's rAF-polled waits crawled, the live-reply reattach failed deterministically, baseline passed it in 4.8s); typecheck, 141/141 unit, build:vite green; conductor 27/27 accounted (13+13 chunked over the 300s cap, chip flake green in isolation twice — the task-131/137 busy-chip flake fails full-file runs on BASELINE too, proven by a stash-restored control run, pre-existing), bridge+away+serial 4/4, routing+smoke+connect-kimi 14/14 (one transient routing:81 flake green alone and in rerun), projects.spec not run (protected); parked-window probe PASS (off-display, unfocused, 479 rAF/2s full speed — same as shown, 240Hz display); two one-off probe scripts used and deleted; both app-token locations held and released; two visible diagnostic windows on the owner's screen disclosed; LOG.md uncommitted per the Task 149 precedent | NO |
| 156 | 2026-07-31 | Standard | Applied | DONE | completed | The cast, for real: the owner-approved faces ported into the live town — new town/faces.ts holds the cast as data (Cairn ready/thinking/working byte-for-byte the app's marks + new done; kimi/codex/claude/gemini with four states, marks, tilt, blink rhythms, color tokens; worker-fallback = the old shared marks in amber) with faceForAdapter mapping by family substring, unknown ids unchanged; TownSquare's TownFace renders from defs (nested eye groups compose blinks, per-face color/glow vars, marks, tilt) and workers wear faceForAdapter(role) in working state (visibility rules untouched); Cairn's done moment: running-to-closed-with-result opens the smile ~6s (aria "Cairn, done"), square-local, model.ts untouched; tokens.css gains --face-* colors+glows, app.css per-face coloring/blink rhythms/bob/tilt with reduced-motion coverage and Cairn's exact original glow kept (one disclosed delta: Cairn mouth now pure garden-cyan per the approved board); lab mock worker runs as codex-exec; 4 new unit tests (mapping, fallback, byte-exact Cairn geometry, four states/distinct colors+blinks per face) = 145/145, typecheck/build:vite/build:lab green; E2E 18/18 with the app token held (smoke/away/connect-kimi/routing+serial/bridge), projects.spec skipped (stopped workers' file), conductor.spec skipped (lane C's active 155 WIP) with a style-inert town-face-worker compat class keeping both its committed and WIP assertions true — removal named for after 155 lands; disclosed — renumbered 154-to-156 after colliding with lane B's claim (their brief restored byte-exact), one transient global-setup mtime flake green on rerun, lab done moment not capturable (mock nulls the session) so logic-verified against tasks.ts; three inspected real-town captures published top of shots page; LOG.md row appended, file still uncommitted per precedent | NO |
| 158 | 2026-07-31 | Standard | Applied | DONE | completed | Remove from this list, finished (owner: pick up the stopped 148/150 envelope work): every remembered project in the picker — healthy or broken — now has a working 'Remove from this list' control that edits only Cairn's remembered list and never the folder on disk; healthy cards restructured (name/milestone stays the open button, stone badge + quiet remove pill on the right; projectForget was already wired for broken entries); the never-before-run suite hid one strict-mode clash — the broken-entry scenario's unscoped remove locator suddenly matched both cards, repaired by scoping it to the broken card (disclosed in the report); typecheck, 145/145 unit, build:vite green; projects.spec 5/5 (12.9s) with BOTH app-token locations held — the new scenario removes Beta through the real UI, proves its AGENTS.md byte-identical before and after, and reopens the untouched folder through projectOpen as the new most-recent entry; RENUMBERED 157→158 after a concurrent lane's brief claimed 157 first (theirs restored byte-exact from 2bf60d8, cmp-verified, no history rewrite); ~11.5 min token wait while that lane's E2E held both locations, then a clean take and release, no processes killed; Picker.tsx CRLF→LF normalized on commit; the Task 156 cast-port and Task 157 follow-ups in-flight files left untouched; LOG.md row appended, file left uncommitted per the Task 149 precedent | NO |
| 157 | 2026-07-31 | Standard | Applied | DONE | completed | Follow-up suggestions on every result card (owner: "add follow up task suggestions from CAIRN whenever a task completes"): the commentary turn now also asks for up to three next steps in a new fail-closed cairn-followups fence (1-3 single-line strings <=140, deduped; replies emitting it are stripped+dropped), persisted on the cairn turn and re-validated on read in store.ts, carried on ConductorDelta; Chat.tsx renders them as dashed chips only while the comment is the last turn — a tap is an ordinary send() (Task 155 queue semantics), never a dispatch, every gate unchanged; phone page unchanged (read-only); 13 new unit pins, 158/158 unit, typecheck, build:vite+build:lab green; conductor.spec 30/30 accounted (22 shard + isolated re-runs + batches) incl. the new chips regression, bridge+away+serial 4/4, 3/3 key tests re-green on the merged tree, projects.spec not run (protected); DISCLOSED — lane C landed 156/158 and claimed 159 mid-task, my app.css styles reached main inside their be248f6 (verified in HEAD), Chat.tsx/spec commits hunk-isolated (1 of 11, 1 of 4) from their in-flight work, 7 orphan electron.exe from another lane's finished suite killed, one :1203 timing flake green in isolation (task-131/137 class), shard killed at the 300s cap and re-chunked; LOG.md uncommitted per the Task 149 precedent | NO |
| 159 | 2026-07-31 | Standard | Applied | DONE | completed | An error card that lets go (owner's stuck-error screenshot): a failed open's card now carries a plain 'Got it', clears with the context that raised it (overlay close, fresh open attempt, project switch), and a contractless CAIRN_OPEN boot lands on a working picker/welcome instead of hanging on 'Getting ready…' (dead-end behavior change disclosed); ErrorCard's dismiss is opt-in so inline cards (Picker/ConnectCard/TaskRun) are unchanged; typecheck, 158/158 unit, build:vite+build:lab green; smoke+projects E2E 8/8 (16.6s) with BOTH app-token locations held incl. two new pins reproducing the owner's exact flow, windows parked off-display; two disclosed test-side repairs — wrong fallback screen asserted (shared profile: picker not welcome), × unclickable under the floating card (now a deliberate disclosed tradeoff: Got it/Escape/scrim still close); Task 157 landed mid-task at cc32c8a via the token protocol, its files untouched; Task 155 WIP and pending LOG rows left uncommitted per the Task 149 precedent; one inspected capture published top of the shots page | NO |
| 155 | 2026-07-31 | Standard | Applied | DONE | completed | A smoother conversation with Cairn — all four owner-approved flow fixes: older result cards fold into one-line aria-expanded chips (only the current moment stays expanded, tap to open/fold away); sends while Cairn answers or comments queue as dimmed take-back-able bubbles and flush in order when the lock frees (quiet first attempt + one 300ms retry against main's finally-after-done lock release; Task 153's refusal path kept as the loud fallback; composer now closes only for runs); Cairn's bubbles shed border+fill, card weight reserved for decisions; tucked chip gains an amber needs-you dot + waiting-decision accessible name; Task 071's refusal test deliberately rewritten as the queue test (owner replaced the behavior), reply-lane queue + needs-you + fold pins added, second-dispatch test's fold gate; renderer-only queue disclosed ephemeral; typecheck, 158/158 unit, build:vite+build:lab green; E2E on the settled tree after the owner chose serialize (lanes 156-159 landed mid-task — 156's commit be248f6 swept this task's app.css lines, disclosed in report; one congested-run town-face flake did not reproduce): conductor 30/30 in five chunks, bridge+away+serial 4/4, routing+smoke+connect-kimi 15/15, projects 6/6 (now an ordinary spec after 158's repair); three inspected fixture captures top of the shots page (queue + needs-you shots at 1320x980, disclosed); throwaway harness deleted; app token held and released | NO |
| 160 | 2026-08-01 | Standard | Applied | DONE | completed | Project checkup (owner: "some kind of button/system that checks the health of a selected project… Looks right, turn it into a task"): every loadable picker card gains a quiet Checkup pill running a read-only, deterministic, no-model audit (new app/src/main/checkup.ts — record pairing/gaps/stale vs in-flight briefs, LOG-row cross-checks, stopped runs filed honestly, unpushed commits >=20 risk / 1-19 attention, dirty/untracked tree, AGENTS.md vs CONTRACT-TEMPLATE.md sync, PROJECT.md cited-version drift) and showing a verdict-first card (new Checkup.tsx) with a one-cell-per-task trail strip and findings grouped Risk / Needs attention / Healthy; every fixable finding carries a suggestion chip that opens the project with the words pre-filled in the composer (App composerSeed -> Workspace consume-once ref -> Chat initialComposer) — never auto-sent, never dispatched, checkup writes nothing; IPC project:checkup through the existing toResult bridge; 11 new unit pins (169/169), typecheck, build:vite+build:lab green; new checkup.spec 1/1 (6.4s) with BOTH app-token locations held — fixture Messy/Tidy projects, fake-conductor connected first (mock boots disconnected), every finding row + trail + unsent-prefill pinned; Cairn self-check honest (Needs a decision: 150 unpushed risk; 160 in flight, 10 dirty + 7 untracked, doc drift attention; contract-in-sync healthy) with one disclosed brief-correction — 148's report-without-brief means "Records intact" correctly stays silent; disclosed repairs: dead rowNoFiles code, PROJECT.md citation regex widened, exact-argv stub keys, trail says "unfinished" not "in flight" for stale briefs, Picker.tsx mixed-CRLF edits made byte-exact via Python after 3 Edit-tool failures (CRLF->LF normalized on commit), E2E spec moved onto the isolated-profile fixture after an early draft used a nonexistent CAIRN_USER_DATA seam (owner's real profile verified unpolluted); LOG.md row appended, file left uncommitted per the Task 149 precedent | NO |
| 161 | 2026-08-01 | Standard | Applied | DONE | completed | Convert an existing project, one button on the projects screen (owner: 'project migration button… Convert an existing non-Cairn project'): the picker card's button runs a read-only inspection (git state, changed/untracked counts, other AI rule files, kept records, legacy .git/cairn warning), shows what Cairn will add, asks the four questions, and converts only on approval — wx-flag creates only, CONVERSION.md report, one exact-path commit when git identity exists, git init for non-repos, hard refusal for foreign AGENTS.md or existing Cairn projects; core convert.ts + 9 core tests (148/148), two IPC handlers + CAIRN_TEST_PICK_FOLDER seam, Convert.tsx panel + Picker wiring, lab mock methods, convert.spec E2E 1/1 (app token held/released), typecheck + 169/169 unit + build:vite/lab green, projects/smoke 9/9 + checkup 1/1 green; disclosed repairs — inspectGit identity probe outside repos, lab mock CairnApi methods, ErrorCard added to main picker (checkup errors had no surface), stale legacy-reset copy replaced; LOG row pooled uncommitted per Task 149 precedent | NO |
| 163 | 2026-08-01 | Standard | Applied | DONE | completed | Added selectable Claude flagship, mid, and fast tiers with verified OpenRouter ids, exact plain pricing, and unchanged consent. (worker claim; files verified a… | NO |
| 164 | 2026-08-02 | Standard | Applied | DONE | completed | Task 163's five owed paths are verified and ready for Cairn's exact-path commit after fresh green gates. (worker claim; files verified against Git by Cairn) | NO |
| 165 | 2026-08-02 | Standard | Applied | DONE | completed | Result cards now clearly explain what was done, what was checked, what the owner should do next, and what still needs judgment. (worker claim; files verified a… | NO |
| 166 | 2026-08-01 | Standard | Applied | DONE | completed | Fixed post-dispatch proposal loss with trusted main-process state: a well-formed third proposal now survives mid-stream Chat reattachment, exact accepted dispatches consume it, refused starts retain it, and 8 affected offline E2E checks pass; Task 155 folding was not the cause and no real model call occurred. | NO |
| 167 | 2026-08-02 | Standard | Applied | DONE | completed | Owner approved the unified ripple-pond Town direction: Cairn is the conversation entry, the live app's faces stay intact, and honest task, question, answer, result, and verification handoffs become visible motion; no app source changed. | NO |
| 168 | 2026-08-02 | Standard | Applied | DONE | completed | The live Town and conversation are now one calm Ripple Pond surface: real approved tasks and results cross between Cairn and the routed worker once, the original faces stay byte-identical, DONE/STOPPED/ERROR remain honest and accessible, 187 unit tests plus 4 focused Electron flows pass, and the owner approved the live feel. | NO |
| 169 | 2026-08-02 | Standard | Applied | DONE | completed | Cairn warms to conductor-v4 (rhythm, never catchphrases - a tic cannot step aside for bad news) and four surfaces stop showing raw codes: "STOPPED - CANCELLED_BY_OWNER" now reads "you stopped it yourself" on the card, the report, and the run strip. The plans final check was "look at it" - every automated check passed and the app still showed the code, because the screenshot was the run strip fed by six serial.ts emit sites, not the card. Mirror test proven to fail on divergence. Eval scenarios 11-12 written, deliberately not run. 198 unit / 151 core / typecheck / both builds; 3 stable E2E failures proven pre-existing (away.spec from Task 161; two verified identical at reverted baseline) | NO |
| 170 | 2026-08-03 | Standard | Applied | DONE | completed | Closed both defects Task 169 surfaced while verifying: away.spec had pinned copy Task 161 removed (it went green the moment the assertion was retargeted, before any source change - the app was right, the test was stale), and the five Check-stage emits on the run screen now say why before they say a code. All eleven stopped-path emits in serial.ts now speak plainly; the run screen keeps the constant in parentheses because it is a debugging surface, the one-line strip does not. Full E2E moved 5 failed/29 passed to 3 failed/38 passed; the three remaining were proven pre-existing. 151 core / 198 unit / typecheck / build, red-first, screenshot inspected, app token held and released | NO |
| 172 | 2026-08-03 | Standard | Applied | DONE | completed | Grounded the conductor in bounded, separately authorized project-file contents while preserving credential, honest-unread-file, and owner-gated dispatch boundaries. | NO |
| 171 | 2026-08-03 | Standard | Applied | DONE | completed | Decision 9 shipped: the conversation is the approved Lantern on Water - warm lit paper resting on dark water instead of a bright rectangle over it - with the owner pastel palette, still water (the three permanently drawn contour rings are gone; a ripple only lands when an event does), New Horizons pills that compress, the town furniture stripped of monospaced HUD labels and dashed threads, and the narrow window resolved as a line or the whole pond, never a shrunken one. Eight defects were found in the PLAN, none in the implementations - three by implementers who stopped rather than guessed, five by reviewers - and three of the eight were one shape: a CSS rule that exists but does not win, guarded by a test checking presence rather than victory, twice across file boundaries. Two regressions were caught only by full verification: the approved lantern sway broke the entire E2E suite (19 failed / 4 passed, element is not stable x60 over 30s - an infinite transform makes every control inside a moving target) and is deliberately NOT shipped; and a second spec still encoded the old narrow behaviour. The whole-branch review then found four more no per-task review could see, including a popover reading through to the conversation and light-theme warning surfaces on permanently dark paper measuring 2.84:1 against a 4.5 floor, fixed with computed contrast. 241 unit / 151 core / typecheck / both builds; E2E 3 failed / 39 passed against a 3-failure baseline with 38 passing, all three proven pre-existing by A/B at the plan base. One trade left open for the owner: on the whole pond a grabbed villager jumps to the shore. | NO |
| 173 | 2026-08-04 | Standard | Applied | DONE | completed | Cairn now captures its selected workspace immediately before a real worker starts and after terminal state paints, leads the authenticated result card with every surviving checked still, and opens a bounded local album on the exact run; app-owned custody, descriptor-bound revalidation, fake-only Electron proof, 306/308 passing app unit tests with 2 platform skips, 151/151 core tests, typecheck, both builds, wide/narrow inspection, and independent reviews passed without a real model call. | NO |
| 174 | 2026-08-04 | Standard | Applied | DONE | completed | Plan 4 now gives uncertainty, hedges, and Cairn-made choices an implementation-ready source path: authenticated owner spans, one-time main previews, exact worker/report/card/phone passage, the deferred What you asked for section, and three independent no-blocker reviews; plan-only, no real model call. | NO |
| 175 | 2026-08-04 | Standard | Applied | DONE | completed | Added Core's immutable source-bound task intent: exact authenticated UTF-16 spans, three source labels, separate context, canonical SHA-256, redacted request views, and fail-closed hostile/boundary validation; 23 focused cases plus full Core, CLI, and App checks passed, additive only with no real model call. | NO |
| 176 | 2026-08-04 | Standard | Applied | DONE | completed | Authenticated exact owner/Cairn turns outside project custody and added one-time main-owned inert actions with fail-closed attribution, replay, parser, and uncertainty handling; fake-only checks passed with no real model call. | NO |
| 177 | 2026-08-04 | Standard | Applied | DONE | completed | Switched App, Core, adapters, records, and CLI to one source-bound accepted intent with atomic one-time preview consumption; 178 Core, 23 CLI, 345 App unit, and 8 guarded Electron checks pass. An early harness error likely invoked installed Kimi once (exit 1; provider contact and cost unknown); the repaired test lane cannot resolve PATH, home, linked, or hostile-shell binaries. | NO |
| 178 | 2026-08-04 | Standard | Applied | DONE | completed | Persisted the source-marked accepted request in whole-card-authenticated DONE, STOPPED, and ERROR cards, preserved explicit null versus legacy absence, and added an exact provider-safe third briefing block; 349 App unit, 178 Core, and 23 CLI tests pass with no Electron or model call. | NO |
| 179 | 2026-08-04 | Standard | Applied | DONE | completed | Desktop questions, proposals, final previews, and authenticated results now share explicit You said so / You weren't sure / Cairn chose attribution, exact quotations, main-owned action authority, and deliberate settlement focus; 361/363 App unit tests pass with 2 platform skips, both builds pass, and no provider or Electron run occurred. | NO |
